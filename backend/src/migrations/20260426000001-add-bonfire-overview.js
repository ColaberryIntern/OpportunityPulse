'use strict';

// Adds bonfire_opportunities.overview — a 2-3 sentence plain-language
// description of what the project actually wants, generated by the AI
// during enrichment. Source `description` / `raw_text` are often thin
// (Bonfire list pages don't carry the full RFP body), so this column
// captures the AI's explanation for human consumption.

module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.addColumn('bonfire_opportunities', 'overview', {
      type: Sequelize.TEXT,
      allowNull: true,
    });
  },

  async down(queryInterface) {
    await queryInterface.removeColumn('bonfire_opportunities', 'overview');
  },
};
