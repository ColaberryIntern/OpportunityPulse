# PROGRESS.md

Implementation progress log for the Opportunity Pulse repository.

This file is the hard-gate verification artifact required by `CLAUDE.md` (see "Logging, Reporting & Progress Tracking"). Every code-touching change to `/backend`, `/frontend`, `/scripts`, `/nginx`, or `/directives` must append an entry here with verification evidence on the same line as the `[x]` mark.

This file was created mid-stream on 2026-05-05; entries before that date are intentionally not back-filled. Forward from this date the gate is enforced.

---

## Deep Research Intelligence Engine — Phase 14 (operational lineage activation + AI quality intelligence)

- [x] Phase 14 — evolves the platform from "Governed operational infrastructure" into "Quality-aware explainable enterprise AI operations". Adds **provenance hot-path wiring** activating Phase 13's cross_provenance + operational_lineage tables across 7 callsites: `actionGenerator.generateOutput` (draftGenerated edge + SSE), `pursuitActivation.activate` (pursuitActivated), `captureStrategy.buildForPursuit` (captureStrategyBuilt), `complianceMatrix.buildForPursuit` (complianceMatrixBuilt), `submissionPackage.assemblePackage` (packageAssembled + SSE), `captureWorker.enqueue` (queueEnqueued + SSE), `captureWorker.recordFailure` (SSE failure publish). Adds **lineageEdgeWriter.service** — one-call helper that emits BOTH a cross_provenance row AND an operational_lineage directed-edge in parallel; 8 domain helpers (pursuitActivated, captureStrategyBuilt, complianceMatrixBuilt, draftGenerated, packageAssembled, workflowAssigned, slaActionTaken, queueEnqueued) + generic `emit()` + `emitChain()`; soft-fails both sides so business logic never breaks on lineage. Adds **proposalQuality.service** — deterministic 8-dimension composite scorer (strategic_alignment 18% / completeness 14% / evaluator_alignment 16% / differentiation 10% / clarity 8% / readiness_consistency 10% / groundedness 14% / operational_coherence 10%) with classification ladder (excellent ≥85 / strong ≥70 / adequate ≥50 / weak); persists to `proposal_quality` with strengths + weaknesses + recommendations. Adds **groundedness.service** — per-claim classification via 3 marker lists (CLAIM_MARKERS / EVIDENCE_MARKERS / WEAK_MARKERS) categorizing every sentence as supported / weak / unsupported / inert; groundedness_score = (supported + weak×0.3) / total_claims × 100; persists unsupported samples for inspection. Adds **strategicCoherence.service** — contradiction detection (8 pre-defined opposing-phrase pairs like 'low cost'+'premium') + pursuit_alignment (priority + differentiator hits) + positioning_alignment + readiness_alignment; coherence_score = pursuit×0.45 + positioning×0.35 + readiness×0.20 − min(30, 15×contradictions). Adds **evaluatorAlignment.service** — deterministic 18-char prefix n-gram match against capture_strategy.evaluator_priorities + recurring_agency_signals + strategic_patterns; alignment_score = round(addressed/total × 80) + min(20, recurring×5 + patterns×5). Adds **qualityOpsDashboard.service** — composite quality score weighting proposal_quality 35% + groundedness 20% + coherence 15% + alignment 15% + explainability 10% + RBAC 5%; status pill (healthy ≥85 / watch ≥60 / elevated ≥40 / critical <40). Adds top-level **AI Quality Operations Center** at `/admin/deep-research/quality-ops` with composite score + 4 dimension breakdowns + top-scoring proposals table + open quality alerts surface. **7 new tables in one migration**: `proposal_quality`, `groundedness_analysis`, `strategic_coherence`, `evaluator_alignment`, `replay_visualizations`, `quality_metrics`, `quality_alerts`. **6 new services + 1 new page**. **15 new admin-only routes** under `/api/v1/deep-research`.
  - Date: 2026-05-16
  - Verification: 23 new pure-logic unit tests in `backend/tests/deepResearch/phase14.test.js` covering proposalQuality WEIGHTS sum to 1.0 + classify ladder + clamp + scoreCompleteness/scoreClarity/scoreDifferentiation/scoreStrategicAlignment + buildRecommendations (8 recs for low scores), groundedness 3 marker lists non-empty + splitIntoSentences + classifySentence (all 4 categories) + hasMarker case-insensitive, strategicCoherence CONTRADICTION_PAIRS structure + detectContradictions positive+negative + detectPursuitAlignment score + neutral-when-no-context + detectReadinessAlignment 100-when-empty, evaluatorAlignment normalize/matches/short-priority-rejection, lineageEdgeWriter helpers surface + emit soft-fail on missing fields. Full Jest suite **2083/2083 tests pass** (+23 net new on top of Phase 13's 2060). Zero regressions across all 186 suites. Migration applied to prod cleanly in `0.468s`; all 7 new tables verified in `information_schema`. Backend `/api/v1/health` returns 200; authenticated call to `/api/v1/deep-research/quality-ops` returns 200 with `{composite_quality_score: 0, status: 'critical', proposal_quality.count: 0, groundedness.count: 0, lineage_health.lineage.total_edges: 0, explainability.coverage_pct: 0}` — expected cold-start state because no Phase 14-scored proposals exist on prod yet. First deploy hit a 500 because Express was matching `/quality/proposal/top` against the `:outputId` param route declared above it; resolved in commit `f0ae2c7` by reordering static routes before the param route. Quality Ops dashboard renders all 10 sections cleanly. 10 screenshots captured from live prod (`docs/deep-research-assets/p14-*.png`). Walkthrough: `docs/deep-research-phase-14-walkthrough.html`.
  - Notes: **Interactive lineage explorer is API-only in v1** — the Phase 13 `GET /operational-lineage/:kind/:id` endpoint returns the BFS graph but a vanilla-SVG render page is deferred to Phase 15. **ReplayTimeline component deferred** — the `replay_visualizations` table exists but the React component is Phase 15. **Quality scoring is opt-in** — no scheduler runs the 4 scorers on every new draft; operators trigger scoring via the API or it happens via a Phase 15 cron. **RBAC migration sprint deferred** — Phase 14 ships the `quality_alerts` table; per-route migration to `requirePermission(...)` is incremental. **quality_alerts auto-generation deferred** — table exists; automatic emitter (e.g. "groundedness < 50" → row) is Phase 15. **Hot-path callsites are 7 of ~12** — still pending: workflowGovernance.transition, slaEscalation.actOnEvent (lineage edge; SSE already wired), proposalReadiness.scorePursuit, reviewQueueBridge. **Cold-start composite score is 0/critical** because no scored proposals exist; every new pursuit-scoped draft generated against prod will immediately produce a non-zero score via the wired hot path. Recommended Phase 15: (1) auto-score scheduler running proposalQuality + groundedness + coherence + alignment on every new OpportunityOutput; (2) interactive lineage explorer page with vanilla-SVG force-directed render; (3) ReplayTimeline component over replay_events rows; (4) auto-alert generator emitting `quality_alerts` rows for low scores; (5) remaining hot-path lineage wiring (workflow + SLA + readiness + review queue); (6) RBAC migration sprint per-route; (7) sparkline trend charts from `quality_metrics` snapshots on the dashboard; (8) per-output quality drawer on Review Queue + Pursuit Workspace pages so quality is visible at the point of operator review.

## Deep Research Intelligence Engine — Phase 13 (cross-phase provenance completion + governance consistency)

- [x] Phase 13 — evolves the platform from "Governed operational infrastructure" into "Fully traceable enterprise AI operational infrastructure". Adds **cross-system provenance** (`crossProvenance.service` + `cross_provenance` table with 17 declared subject kinds × 14 provenance kinds; answers "why does this exist / what created it / what context produced it" for every operational object; per-subject + per-pursuit lookups). Adds **full operational lineage graph** (`operationalLineage.service` + `operational_lineage` denormalized directed-edge table with bounded BFS traversal up to depth 6 / 250 nodes; `proposalAncestry()` walks back from an OpportunityOutput through prompt_provenance → pursuit → cross_provenance sources). Adds **permission integrity** (`permissionIntegrity.service` + `permission_integrity` snapshot table; per-user RBAC audit detecting over-permissioned (≥90% of all perms granted, non-super_admin), orphan grants (OperatorPermission rows whose User is gone), stale grants (>90 days); integrity_score = 100 - 5×orphan - 4×over - 2×stale; mismatch report with severity-ranked recommendations). Adds **governance consistency engine** (`governanceConsistency.service` + `governance_consistency` table with 7 deterministic checks: stale_open_sla, orphan_workflow, lineage_gap, approval_bypass, conflicting_approval, invalid_transition, lineage_governance_gap; idempotent findings via (check_kind, subject) — re-scans bump severity but don't duplicate; severity-weighted consistency_score; **recommendation-only — never auto-corrects governance state**). Adds **end-to-end proposal explainability** (`proposalExplainability.service.buildExplanation(outputId)` returns 7 named sections: output / prompt_provenance / pursuit / capture_strategy / readiness_blockers / cross_provenance / operator_actions + a deterministically-rendered narrative; **no LLM inference involved in the explanation itself**). Adds **governance drift detection** (`governanceDrift.service` + `governance_drift` table with 4 active scanners + 2 reserved kinds: permission_drift (unknown role names in OperatorPermission), workflow_drift (assignments stuck >5 days, severity scales with age), escalation_drift (SLA events at severity ≥75 still open), policy_drift (tenant in non-standard governance_mode without recent tenant.update audit event); **recommendation-only**). Adds **approval workflow provenance** (`approvalProvenance.service` + `approval_lineage` append-only table; `backfillFromWorkflows()` synthesizes lineage from existing WorkflowAssignment.history JSONB idempotently; bottleneck detector ranks by durationSeconds; override tracker; `humanizeSeconds()` for UI rendering). Adds **operational replay** (`operationalReplay.service` chronologically merges audit_events + approval_lineage + cross_provenance + sla_acknowledgements + governance_events for 4 scopes (pursuit / output / workflow / sla_event); optional `persistReplay()` to `replay_events` table; **READ-ONLY — never mutates state**). Adds **SSE stream integrity** (`streamIntegrity.service` per-org counters for heartbeats / published / dropped / duplicate_suppressed / reconnects + peak concurrent + backpressure %; `snapshot()` flushes counters to `stream_integrity` table; integrity_score = 100 - drop_rate%). Adds top-level **Enterprise Governance Assurance Center** dashboard at `/admin/deep-research/governance-assurance` with composite 0-100 trust score weighting explainability (20%) + consistency (25%) + permission (20%) + stream (15%) + drift (20%); status pill (healthy ≥85 / watch ≥60 / elevated ≥40 / critical <40); also surfaces approval-provenance summary + replay registry. **8 new tables in one migration** + Phase 12 follow-up backfill of `organization_id` on `opportunity_outputs`: `cross_provenance`, `operational_lineage`, `governance_drift`, `approval_lineage`, `replay_events`, `permission_integrity`, `stream_integrity`, `governance_consistency`. **10 new services**. **27 new admin-only routes** mounted under `/api/v1/deep-research`.
  - Date: 2026-05-16
  - Verification: 17 new pure-logic unit tests in `backend/tests/deepResearch/phase13.test.js` covering crossProvenance VALID_SUBJECT_KINDS + VALID_PROVENANCE_KINDS surfaces, operationalLineage MAX_DEPTH/MAX_NODES positive ints + VALID_NODE_KINDS + VALID_RELATIONS, governanceConsistency VALID_CHECKS + STALE_DAYS_SLA + recordFinding invalid-kind soft-fail, governanceDrift VALID_DRIFT_KINDS exact 6-element list + STUCK_WORKFLOW_DAYS + ESCALATING_SEVERITY_THRESHOLD + recordDrift invalid-kind soft-fail, approvalProvenance VALID_ACTIONS + humanizeSeconds formats (s/m/h/d), operationalReplay VALID_SCOPES + buildReplay invalid-scope BAD_INPUT rejection, streamIntegrity counter helpers no-throw, permissionIntegrity STALE_DAYS + OVER_PERMISSION_THRESHOLD + surface. Full Jest suite **2060/2060 tests pass** (+17 net new on top of Phase 12's 2043). Zero regressions across all 185 suites. Migration applied to prod cleanly in `0.736s`; all 8 new tables + opportunity_outputs.organization_id verified in `information_schema`. Backend `/api/v1/health` returns 200 healthy; authenticated call to `/api/v1/deep-research/governance-assurance` returns `{assurance_score: 80, status: 'watch', sub_scores: {explainability_coverage: 0, consistency: 100, permission_integrity: 100, stream_integrity: 100, drift_pressure: 100}}` — full end-to-end working with the watch status from explainability=0 (no outputs are linked to provenance yet on prod) but all other sub-scores at 100. Governance Assurance dashboard renders all 10 sections cleanly. 10 screenshots captured from live prod (`docs/deep-research-assets/p13-*.png`): full page, operator actions, assurance composite (80/100 watch), cross provenance, operational lineage, permission integrity, consistency, explainability, drift, stream integrity. Walkthrough: `docs/deep-research-phase-13-walkthrough.html` (governance contract block, architecture per feature, 10 screen sections, schema + routes tables, validation report, known limitations, recommended Phase 14).
  - Notes: **Cross-provenance callsite wiring is partial** — the table + APIs exist but only a few callsites currently call `crossProvenance.record()` directly; Phase 14 wires it into pursuit activation, capture strategy build, package assembly, compliance matrix build, queue enqueue, SLA action paths. **Operational lineage edges are not auto-populated** on every relevant action yet — Phase 14 adds edge writes at the same hot-paths as cross-provenance. **2 of 6 drift kinds (approval_drift, tenant_config_drift) have no scanner yet** — declared in VALID_DRIFT_KINDS but inactive; Phase 14 lights them up. **Replay UI** is API-only in v1; an interactive lineage explorer is Phase 14. **Stream integrity counters** are recorded via direct service calls + persisted via snapshot, but the SSE service itself (Phase 11 observabilityStream) doesn't yet call the counter helpers on drop / duplicate / reconnect events — that self-wiring is Phase 14. **Per-feature RBAC migration** from legacy `checkPermissions(ROLES.ADMIN)` to `requirePermission(...)` still pending — Phase 12 coverage reporter exists; Phase 14 executes the migration route-by-route. **explainability_coverage=0 on prod cold-start** because no OpportunityOutput rows have a non-null promptProvenanceId yet (Phase 12 wired the prepend into actionGenerator but no Phase 12-aware drafts have been generated against prod yet). Recommended Phase 14: (1) cross-provenance + operational-lineage callsite wiring across all Phase 8/9/10/11 hot paths; (2) activate approval_drift + tenant_config_drift scanners; (3) interactive lineage explorer page with BFS graph render; (4) stream integrity self-wiring (observabilityStream calls streamIntegrity.recordDropped / recordDuplicate / recordReconnect internally); (5) RBAC route migration sprint using Phase 12 coverage report as punch-list; (6) replay export CSV/JSON for compliance reviews; (7) per-tenant assurance trend charts from the 3 snapshot tables; (8) wire reviewQueueBridge through actionGenerator with pursuitId so backfilled drafts also gain provenance.

## Deep Research Intelligence Engine — Phase 12 (cross-phase tenant backfill + LLM provenance hardening)

- [x] Phase 12 — evolves the platform from "Enterprise-governed infrastructure" into "Tenant-safe, auditable AI operational infrastructure". Adds **cross-phase tenant backfill** (single idempotent migration adds `organization_id` with default-to-1 backfill to all 8 Phase 10 tables: `worker_jobs`, `proposal_execution_queue`, `artifact_lifecycle_events`, `sla_events`, `queue_metrics`, `storage_assets`, `execution_failures`, `operational_metrics`; Phase 10 model definitions extended with the field; governance dashboard + slaEscalation + slaIntelligence.upsertEvent all restored to use real tenant filters). Adds **prompt provenance hardening** (`promptProvenance.service` + `prompt_provenance` table with append-only audit_hash log; every prompt-context block recorded with audit_hash, prompt_version, included/excluded sections, char_length, block_preview, generated_by; `findByOutput` / `findByAuditHash` / `listForPursuit` reverse-lookup APIs; OpportunityOutput extended with `prompt_provenance_id` + `prompt_audit_hash` columns). Adds **real prompt context injection** (`actionGenerator.generateOutput` now accepts optional `pursuitId`; when present, `promptProvenance.buildAndPersist()` composes the deterministic block, prepends to `buildGroundedUserPrompt`, persists provenance, stamps both columns on the OpportunityOutput, and `promptProvenance.linkOutput` closes the loop). Adds **full RBAC route coverage reporter** (`rbacCoverage.service.analyzeRouter` walks the Express deepResearch router's stack and classifies each layer as `requires_permission` / `legacy_admin` / `unprotected`; `rbac.requirePermission` middleware now stamps `_permission` on the guard fn so coverage is inspectable; `rbac_coverage` snapshot table). Adds **UI permission synchronization** (`hooks/usePermissions.js` React hook with 5-min TTL in-memory cache + `can(perm)` helper + fail-closed observer-default; `components/common/Permitted.jsx` wrapper with `name` / `anyOf` / `allOf` / `role` / `minRoleLevel` props; backend remains the source of truth). Adds **SLA escalation email pipeline** (`slaDigest.service.sendForOrg` composes per-tenant digest from `slaEscalation.generateDigest`, ships via existing `utils/email` plumbing, records every attempt to `sla_email_events`; recipients resolved from `DEEP_RESEARCH_SLA_DIGEST_RECIPIENTS` env or org User lookup; `dryRun=true` for preview; soft-fail on missing mailer). Adds **SSE hot-path wiring** (`sseHotPaths.service` wraps Phase 11 `observabilityStream.publishers` with per-channel counters; `snapshotMetrics` persists rolling 5-min counters to `stream_metrics`; wired into actionGenerator.generateOutput as `draft.complete` for v1). Adds **S3/MinIO asset migration** (`assetMigration.service` idempotent + resumable url_only → configured-provider migration; hash validation on bytes when present; per-attempt log in `asset_migrations`; **never deletes the original**). Adds **audit retention infrastructure** (`auditRetention.service` configurable per-tenant retention via `TenantSettings.retentionDays` default 365; `retentionPressure` indicator with status (healthy/watch/elevated/critical); `recommendArchive` returns the next archive window; `archiveWindow` writes a content-hashed manifest to `audit_archives`; **never deletes source rows in v1**). Adds top-level **Governance Integrity Operations Center** dashboard at `/admin/deep-research/governance-integrity` with a composite 0-100 trust score weighting tenant isolation (25%) + RBAC (20%) + provenance (20%) + observability (15%) + retention (20%); status pill (healthy ≥85 / watch ≥60 / elevated ≥40 / critical <40); snapshots to `governance_integrity` table. **8 new tables in one migration**: `prompt_provenance`, `asset_migrations`, `rbac_coverage`, `governance_integrity`, `sla_email_events`, `audit_archives`, `permission_cache`, `stream_metrics`. **7 new services + 1 hook + 1 wrapper component**. **25 new admin-only routes** mounted under `/api/v1/deep-research`. Frontend gets new `Integrity Operations` sidebar link.
  - Date: 2026-05-16
  - Verification: 21 new pure-logic unit tests in `backend/tests/deepResearch/phase12.test.js` covering promptProvenance surface + constants, rbacCoverage analyzer empty/null/single-route classification + requires_permission detection + legacy_admin detection, slaDigest defaultRecipients env parsing + formatBody counts/critical/recommendation + empty-critical-list + SUBJECT_PREFIX, sseHotPaths counter increment + publisher channel coverage + summary shape, assetMigration VALID_STATUSES + surface, auditRetention DEFAULT_RETENTION_DAYS + ARCHIVE_BATCH_SIZE positive ints + archiveWindow BAD_INPUT rejection, governanceIntegrity PHASE_10_TABLES_WITH_ORG_ID list + aggregator surface. Full Jest suite **2043/2043 tests pass** (+21 net new on top of Phase 11's 2022). Zero regressions across all 184 suites. Migration applied to prod cleanly in `0.635s`; all 8 new tables verified in `information_schema`; all 8 Phase 10 tables now carry `organization_id` (verified via `information_schema.columns`). Backend `/api/v1/health` returns 200; authenticated call to `/api/v1/deep-research/governance-integrity` returns `{integrity_score: 60, sub_scores: {tenant_isolation: 100, rbac: 0, provenance: 0, observability: 100, retention: 100}}` — the **tenant_isolation=100** confirms the Phase 10 backfill is complete and queryable. First deploy hit a 500 because `provenanceScore` filtered `opportunity_outputs` by `organizationId` (column doesn't exist on that legacy table); resolved in commit `a5a7bea` by reporting platform-wide provenance coverage for v1. Governance Integrity dashboard renders all 9 sections cleanly on prod. 10 screenshots captured from live prod (`docs/deep-research-assets/p12-*.png`): full page, operator actions, integrity composite (60/100 watch status), RBAC coverage, prompt provenance, asset migration, audit retention, SLA email pipeline, SSE throughput, post-snapshot full page. Walkthrough: `docs/deep-research-phase-12-walkthrough.html` (governance contract block, architecture diagrams per feature, 10 screen sections, schema + routes tables, validation report, known limitations, recommended Phase 13).
  - Notes: **`opportunity_outputs` still has no `organization_id` column** — provenance coverage is reported platform-wide in v1; Phase 13 ships that backfill. **SSE hot-path wiring is partial** — `draft.complete` is wired in actionGenerator; `worker.state`, `queue.update`, `package.assembled`, `failure` still need one-line callsite additions in captureWorker tick handlers + submissionPackage. **S3 byte migration is opt-in** via `copyBytes` and requires the asset to carry a base64 buffer in metadata; a real one-shot migration script that reads url_only content_refs and uploads them is Phase 13. **SLA digest scheduling** is operator-triggered for v1; daily 8AM org-local cron is Phase 13. **RBAC route migration** is reporter-only in v1; per-feature migration to `requirePermission(...)` is the Phase 13 sprint. **Audit archive cold-storage write** records manifest + hash; the actual S3 export + safe-delete of source rows is Phase 13. **permission_cache table** is created but not yet used — `usePermissions` hook caches in-process for now; backing the hook with the table for cross-process sharing is Phase 13. Recommended Phase 13: (1) opportunity_outputs org_id backfill migration; (2) full SSE hot-path wiring in captureWorker + submissionPackage + complianceMatrix; (3) per-feature RBAC route migration using the Phase 12 coverage report as punch-list; (4) real S3 byte migration script; (5) daily SLA digest cron; (6) audit archive cold-storage export + safe-delete; (7) backed permission_cache; (8) pursuit-context-injection wiring through reviewQueueBridge + all remaining draft pathways.

## Deep Research Intelligence Engine — Phase 11 (multi-tenant governance + operational auditability)

- [x] Phase 11 — evolves the platform from "Operational capture infrastructure" into "Enterprise-governed strategic capture infrastructure". Adds **multi-tenant isolation** (`tenantIsolation.service` — org_id resolution from JWT or User lookup, `tenantMiddleware` stamps `req.tenantId` per request, `applyTenantScope` + `assertOwnership` helpers, per-tenant `TenantSettings` with governance mode + SLA threshold overrides + approval-required list + storage provider + feature flags + retention, strict-isolation mode raises `TENANT_DENIED`, loose mode logs and continues for backward-compat with the seeded default org=1). Adds **enterprise RBAC** (`rbac.service` — 6 roles (observer/reviewer/proposal_writer/capture_manager/org_admin/super_admin) on an ordered ladder, ~30 declared permissions, per-role default permission map, per-user `OperatorPermission` overlay with grant/revoke audit, `legacy ROLES.ADMIN → super_admin` bridge so every existing admin-gated route keeps working, `requirePermission(name)` middleware factory). Adds **append-only audit trail** (`auditTrail.service` — every operator + system action with actor email + role + IP + UA + correlation_id, queryable by tenant/actor/pursuit/kind/verb/date, CSV export, `recordFromRequest(req, ...)` one-line sugar for controllers). Adds **operational event lineage** (`eventLineage.service` — directed edges between 22 entity kinds × 14 edge kinds, bounded BFS `chain()` traversal up to depth 6, `fullLineage()` upstream+downstream view, per-pursuit timeline). Adds **SLA escalation workflow** (`slaEscalation.service` — 6 actions ack/assign/escalate/reassign/resolve/dismiss/note over Phase 10 sla_events, every action creates an `SlaAcknowledgement` audit row, escalation ladder observer→org_admin, auto-creates `WorkflowAssignment` rows on assign, daily digest payload generator, **recommendation-only — no auto-resolve, no auto-punishment**). Adds **real storage provider adapters** (`storageProviders.service` — S3/MinIO/local-disk layered over the Phase 10 url_only registry, lazy `aws-sdk` loading so the platform never crashes for missing deps, signed URLs delegated, soft-fail `deleteBytes` so metadata cleanup proceeds, `migrationStatus` reports rows needing a one-off byte copy). Adds **real-time observability stream** (`observabilityStream.service` — SSE bus with 9 declared channels worker.state/queue.update/sla.alert/readiness.change/draft.complete/package.assembled/failure/governance.event/workflow.transition, tenant-isolated, throttled 250ms min per channel, reconnect-safe 100-event ring buffer, heartbeats every 25s, in-DB `observability_streams` subscriber registry). Adds **operator workflow governance** (`workflowGovernance.service` — 7 workflow kinds, full state machine open→ack→in_progress→{approved/rejected/completed/cancelled}, per-assignment history JSONB, workload-by-operator rollup, bottleneck detector by avg-age, approval-aging report, every transition writes a `GovernanceEvent` + audit row). Adds **deterministic pursuit-context prompt composition** (`pursuitContextPrompt.service` — markdown block composed from named Phase 8/9 sources (pursuit + capture strategy + readiness blockers + linked opps + suggested assets + strategic patterns + recurring agency), char-budget aware default 4500, governance footer telling the LLM not to invent stakeholders, `audit_hash` sha256-16 on every block so the exact prompt that shipped with each draft is reproducible, **no hidden prompt augmentation**). Adds top-level **Enterprise Capture Governance Center** dashboard at `/admin/deep-research/governance` aggregating tenant pressure (0-100 composite score), SLA escalations, operator workloads, audit timeline, workflow bottlenecks, approval aging, queue governance, storage health, SSE stream registry, lineage overview, and recent governance events. **8 new tables in one migration**: `audit_events`, `event_lineage`, `workflow_assignments`, `sla_acknowledgements`, `tenant_settings`, `operator_permissions`, `governance_events`, `observability_streams`. **10 new services** in `backend/src/deepResearch/`. **26 new admin-only routes** mounted under `/api/v1/deep-research/governance/*`. Tenant middleware threaded onto the admin chain so every controller has `req.tenantId` before business logic runs.
  - Date: 2026-05-16
  - Verification: 40 new pure-logic unit tests in `backend/tests/deepResearch/phase11.test.js` covering tenantIsolation (applyTenantScope camel + raw, assertOwnership strict-vs-loose, DEFAULT_ORG_ID positive int), rbac (ROLE_NAMES order, levelOf ranking, normalizeLegacyRole admin→super_admin, super_admin has every permission, observer read-only, proposal_writer can draft but not approve, capture_manager can cancel queue but not grant, explicit permission overrides, PERMISSIONS uniqueness), auditTrail (VALID_KINDS + COMMON_VERBS surfaces), eventLineage (VALID_KINDS + EDGE_KINDS + MAX_DEPTH), slaEscalation (VALID_ACTIONS, ESCALATION_LADDER ordering, nextEscalationRole advancement), workflowGovernance (VALID_WORKFLOW_KINDS + VALID_STATUSES), storageProviders (SUPPORTED_PROVIDERS, configuredProvider fallback to url_only on missing/invalid env, providerHealth url_only healthy path), observabilityStream (VALID_CHANNELS, summarize zero-state, publish ignores unknown channels, publish 0-delivered when no subscribers, throttle within MIN_EMIT_INTERVAL_MS, publishers helper proxies), pursuitContextPrompt (composePromptBlock null-context empty path, capture-strategy sections present, char-budget truncation, governance NOTE footer, MAX_BLOCK_CHARS range). Full Jest suite **2022/2022 tests pass** (+40 net new on top of Phase 10's 1982). Zero regressions across all 183 suites. Migration applied to prod cleanly in `0.447s`; all 8 tables verified in `information_schema`. Backend `/api/v1/health` returns 200 healthy; `/api/v1/deep-research/governance` returns a fully populated composite — first deploy hit a 500 because the Phase 11 aggregator filtered the Phase 10 `sla_events` table by `organization_id` (which it doesn't have yet); resolved in commit `063b3fa` by removing the filter on un-scoped Phase 10 tables. Governance dashboard renders all 11 sections cleanly on prod. 10 screenshots captured from live prod (`docs/deep-research-assets/p11-*.png`): full page, tenant pressure, SLA escalations, operator workloads, workflow bottlenecks, approval aging, audit trail, storage backend, observability streams, governance events. Walkthrough: `docs/deep-research-phase-11-walkthrough.html` (governance contract block, architecture diagrams per feature, 10 screen sections, schema + routes tables, validation report, known limitations, recommended Phase 12).
  - Notes: **8 Phase 10 tables (sla_events, worker_jobs, proposal_execution_queue, artifact_lifecycle_events, queue_metrics, storage_assets, execution_failures, operational_metrics) do NOT have `organization_id` yet** — Phase 11 governance reads them platform-wide. A Phase 12 migration adds org_id to all of them with a default backfill to org 1. **S3/MinIO providers are wired but not migrated** — adapter calls work, no existing url_only asset has been copied yet; Phase 12 ships the one-off byte-migration script. **SLA email escalation** — daily digest payload is built; actual email send is queued for Phase 12. **SSE publish-side wiring** is partial — wired on SLA actions and workflow transitions; Phase 12 wires the rest (worker.state, queue.update, draft.complete, package.assembled, failure). **Pursuit-context prompt** is previewable + audit-hashed but the actual prepend into `actionGenerator.buildGroundedUserPrompt` is staged separately so the governance gate can be flipped before any LLM behavior changes. **RBAC middleware is built but routes still use legacy `checkPermissions(ROLES.ADMIN)`** — `legacy admin → super_admin` bridge keeps everything working; per-feature migration to `requirePermission(...)` is Phase 12. Recommended Phase 12: (1) Phase 10 tables org_id backfill migration; (2) real `actionGenerator` prompt prepend; (3) per-route RBAC migration; (4) S3 byte migration script; (5) SLA email pipeline via Source Health Agent; (6) SSE publish on all Phase 10 hot paths; (7) UI permission gating via `usePermissions()` hook; (8) audit log retention auto-archive.

## Deep Research Intelligence Engine — Phase 10 (operational scalability + execution infrastructure)

- [x] Phase 10 — evolves the platform from "Capture operations intelligence" into "Operational capture infrastructure". Adds a durable polling worker over a new `worker_jobs` queue (6 job kinds: draft_generation, compliance_parse, package_assembly, artifact_lifecycle_scan, sla_scan, storage_upload) with retry-safety, exponential backoff + jitter, optimistic-update job acquisition, typed failure classification (timeout / permanent / config / upstream / transient), bounded concurrency, opt-in scheduler. New high-level `proposal_execution_queue` for operator-facing draft batches + package assemblies + compliance parses that fan out into N worker_jobs via batch_id, with status rollup + idempotent enqueue + cancellation. SLA Intelligence with 6 deterministic scanners (gap_aging, pursuit_stagnant, draft_stalled, queue_aging, readiness_stagnant, blocker_aging) — severity scales with threshold overshoot, escalation copy per kind, idempotent upsert ensures one open event per (kind, scope). Artifact Lifecycle automation flips proposal_artifacts between active ↔ expiring ↔ expired on a nightly scan with append-only `artifact_lifecycle_events` and ranked renewal recommendations. Queue Observability computes per-kind p50/p95 latency + retry rate + failures-by-class over a rolling 60-minute window, with periodic `queue_metrics` snapshots and a worker health composite. Storage abstraction registers files via the new `storage_assets` table with 4 declared backends (s3 / minio / local / url_only); v1 defaults to url_only so deploys need no S3 config. Pursuit context wiring stamps the Phase 9 context block onto OpportunityOutput.metadata at draft time (full prompt-prepend deferred to Phase 11). Durable Draft Generation replaces Phase 8's in-process parallelDraftQueue with worker-backed jobs — resumable, retry-safe, idempotent. **Optional LLM Compliance Augment** is strictly feature-flagged (default OFF) — the deterministic Phase 9 regex parser remains primary; LLM only fills gaps in thin matrices and tags every item with metadata.source. New top-level **Capture Infrastructure** dashboard at `/admin/deep-research/capture-infra` aggregates worker health + queue throughput + SLA pressure + artifact lifecycle + execution queue + recent jobs + failures + storage backend, with operator-triggered SLA scan / lifecycle scan / queue snapshot / worker drain. **8 new tables in one migration**: `worker_jobs`, `proposal_execution_queue`, `artifact_lifecycle_events`, `sla_events`, `queue_metrics`, `storage_assets`, `execution_failures`, `operational_metrics`. **10 new services** in `backend/src/deepResearch/`: `captureWorker`, `proposalExecutionQueue`, `slaIntelligence`, `artifactLifecycle`, `queueObservability`, `storage`, `pursuitContextWiring`, `durableDraftGeneration`, `complianceMatrixLlm`, `captureInfra`. **27 new admin-only routes** mounted under `/api/v1/deep-research`. Governance contract preserved verbatim: no auto-submit, no auto-bid, no auto-sign; LLM augments / deterministic parser primary; every job persists full payload + attempt count + typed failure class for audit.
  - Date: 2026-05-16
  - Verification: 21 new pure-logic unit tests in `backend/tests/deepResearch/phase10.test.js` covering captureWorker (classifyFailureKind buckets, backoffMs exp+jitter bounded, newBatchId uniqueness, VALID_JOB_KINDS), storage (activeProvider fallback to url_only, VALID_PROVIDERS, isMimeAllowed), slaIntelligence (severityForAge scaling, escalationFor per-kind, VALID_KINDS, THRESHOLDS one-entry-per-scanner), artifactLifecycle (VALID_EVENT_KINDS canonical order, EXPIRING_WINDOW_DAYS), queueObservability (median + p95 edge cases, VALID_KINDS mirrors captureWorker), complianceMatrixLlm (FEATURE_FLAG_ENV stable + default-disabled, isEnabled exact-string-match, sanitizeLlmItems coerce/validate/cap-at-MAX, empty-safe), proposalExecutionQueue (VALID_KINDS + VALID_STATUSES), durableDraftGeneration (SUPPORTED_TYPES). Full Jest suite **1982/1982 tests pass** (+21 net new on top of Phase 9's 1961). Zero regressions across all 182 suites. Migration applied to prod cleanly in `0.443s`; all 8 tables verified in `information_schema`. Backend `/api/v1/health` returns 200 healthy after deploy; `/api/v1/deep-research/capture-infra` returns 401 (admin-gated, route wired). Capture Infrastructure dashboard renders end-to-end on prod with all 8 sections — first deploy had a React error #31 from queue.by_kind shape mismatch; fixed by aligning the page to the real `queueObservability.summarize()` array shape (commit `4e6ab7f`). 10 screenshots captured from live prod (`docs/deep-research-assets/p10-*.png`): full page, operator actions, durable worker, queue throughput, SLA pressure, artifact lifecycle, execution queue, recent jobs, failures, storage. Walkthrough: `docs/deep-research-phase-10-walkthrough.html` (governance contract block, mechanics section per feature, 10 screen sections, schema + routes tables, validation report, known limitations, recommended Phase 11).
  - Notes: Worker scheduler is opt-in via `DEEP_RESEARCH_WORKER_ENABLED=true` — default OFF in prod so the queue + worker can be smoke-tested deliberately before activation. LLM compliance augment is opt-in via `DEEP_RESEARCH_LLM_COMPLIANCE_ENABLED=true` — preserves deterministic-first governance. Storage layer ships with `url_only` provider as the only fully-wired backend; s3/minio/local are placeholder stubs queued for Phase 11. Pursuit context wiring stamps the block onto OpportunityOutput.metadata rather than prepending into actionGenerator's prompt builder — full prompt integration is Phase 11. SLA emails to operators (via the daily Source Health Agent email pipeline) deferred to Phase 11. Dashboard is poll-on-refresh; no WebSocket / SSE push. Recommended Phase 11: (1) S3/MinIO storage provider wiring + asset migration; (2) actionGenerator prompt-builder integration of pursuit context block; (3) SLA email escalation via Source Health Agent; (4) multi-tenant scoping (organization_id row-level filtering); (5) submission audit trail (append-only operator-action log per pursuit); (6) Capture Infra trend sparklines from queue_metrics; (7) Compliance Matrix LLM augment UI button on Pursuit Workspace; (8) WebSocket / SSE push for live dashboard updates.

## Deep Research Intelligence Engine — Phase 9 (submission readiness + compliance intelligence)

- [x] Phase 9 — evolves the platform from "Strategic capture intelligence" into "Submission readiness intelligence". Adds an RFP attachment locker (metadata + versioning), a reusable proposal artifact vault (9 kinds + expiration tracking + suggest-for-context), a deterministic compliance matrix generator (12-pattern RFP text parser + 10-item default template + per-item status workflow with auto-rollup), a submission package assembler (drafts + attachments + reusable artifacts → 0-100 completeness score), a pursuit-aware proposal context injector (prompt-ready block carrying capture strategy + linked opps + acceleration assets + readiness blockers), a parallel draft generation queue (bounded concurrency 3-8, idempotent, retry-safe, observable), an append-only proposal readiness timeline (8-event default template + overdue auto-detection), a compliance gap detector (6 gap kinds × severity + recommended actions), and a top-level Capture Operations Center dashboard at `/admin/deep-research/capture-ops`. The Phase 9 submission readiness engine composites 7 dimensions (compliance 25 + attachments 15 + artifacts 15 + package 15 + staffing 10 + capability 10 + timeline 10 = 100) and persists the result into the Phase 8 `proposal_readiness_scores` table (scope=pursuit) so Capture Ops reads it without a new join. **8 new tables in one migration**: `rfp_attachments`, `proposal_artifacts`, `compliance_matrices`, `compliance_matrix_items`, `submission_packages`, `proposal_timeline_events`, `compliance_gaps`, `parallel_draft_jobs`. **10 new services** in `backend/src/deepResearch/`: `rfpAttachment`, `proposalArtifact`, `complianceMatrix`, `submissionPackage`, `pursuitContextInjector`, `parallelDraftQueue`, `proposalTimeline`, `complianceGap`, `submissionReadinessEngine`, `captureOps`. **27 new admin-only routes**. New `CaptureOpsPage.jsx` with KPI strip + active-pursuits list + gaps-by-pursuit + artifact health + package snapshot + timeline rollup + draft queue. `PursuitWorkspacePage` gains 7 new panels: Submission Readiness Composite (Phase 9), Compliance Matrix, Compliance Gaps, RFP Attachment Locker, Proposal Timeline, Submission Packages, Parallel Draft Generation. **Recommendation-only end-to-end** — no auto-submit, no auto-bid, no auto-sign, no auto-modify compliance docs.
  - Date: 2026-05-16
  - Verification: 30 new pure-logic tests in `backend/tests/deepResearch/phase9.test.js` covering rfpAttachment VALID_KINDS + missing-kinds detection, proposalArtifact whitelists, complianceMatrix RFP parsing (default template fallback + SAM.gov/capability statement/key personnel detection + SF form extraction + page-limit extraction + roll-up math), submissionPackage completeness scoring (empty/full/partial), proposalTimeline event-kind whitelist, complianceGap severity per gap_kind + boost-for-critical + recommended actions, submissionReadinessEngine WEIGHTS sum-to-100 + classify thresholds, parallelDraftQueue SUPPORTED_TYPES + newBatchId + MAX_CONCURRENCY=8, pursuitContextInjector composeContextBlock null/empty/full paths + fmtList truncation. Full Jest suite **1961/1961 tests pass** (+30 net new on top of Phase 8's 1931). Zero regressions across all 181 suites. Migration ran clean on prod (`migrated 0.527s`); all 8 tables verified. Full end-to-end Phase 9 lifecycle validated on prod (pursuit #3): built compliance matrix from sample RFP text ("Submit via SAM.gov by 30 days. Include capability statement and SF-1449. Key personnel resumes required. NAICS 541512.") → 14 items extracted; seeded 8-event default timeline; detected 16 compliance gaps; assembled submission package (0% complete with 4 missing components flagged); scored Phase 9 submission readiness composite = 22.21 / 100 / classification=blocked / 10 blockers; Capture Ops dashboard returned 3 active pursuits with mean readiness 22.2. 10 screenshots captured from live prod across the Capture Operations Center (full + KPIs + gaps section) and the pursuit workspace (full + Compliance Matrix + Compliance Gaps + Submission Readiness Composite + Timeline + Submission Packages + Parallel Draft Generation panels) — `docs/deep-research-assets/p9-*.png`. Walkthrough: `docs/deep-research-phase-9-walkthrough.html` (governance contract block, mechanics section per feature, 10 screen sections, schema + routes tables, validation report, known limitations, recommended Phase 10).
  - Notes: RFP attachment locker is metadata-only in v1 — no file blob storage. Operators paste a content_ref URL (or share link). Real S3/MinIO blob storage is a clean Phase 10 add. Compliance matrix parser is regex-based with 12 patterns; misses paraphrased/table-formatted requirements (Phase 10 could add an optional LLM second-pass behind a feature flag). Parallel draft pool runs in-process inside the HTTP request handler — large batches (>20 opps) can hit the nginx 60s timeout (Phase 10 will move it to a background worker). Pursuit context injector composes the prompt-ready block at `GET /pursuits/:id/context-block` but actionGenerator wiring is deferred — extending actionGenerator to accept an optional `pursuitContext` arg is a small Phase 10 add. Artifact expiration refresh is manual (button on Capture Ops dashboard); a nightly cron is a clean Phase 10 add. Recommended Phase 10 #1: real file storage (S3) behind content_ref. #2: background worker for parallel drafts. #3: actionGenerator pursuit-context wiring. #4: optional LLM compliance-matrix second-pass. #5: gap aging + SLA tracking. #6: artifact expiration scheduler. #7: outcome learning loop (submitted → won/lost → readiness threshold tuning).

## Deep Research Intelligence Engine — Phase 8 (pursuit activation + strategic capture intelligence)

- [x] Phase 8 — evolves the platform from "Strategic opportunity intelligence" into "Strategic capture intelligence". Every Phase 1-7 surface now ends with an **Activate Pursuit** button; every pursuit gains four new panels (Generate Drafts, Proposal Readiness, Capture Strategy, Submission Readiness); My Opportunities in strategic mode gains 8-tab Strategic Workspace navigation; the Action Intelligence dashboard gains a force-directed Opportunity Graph. All measure-only — no auto-submit, no auto-bid, no auto-launch. **8 new tables in one migration** (pursuit_activations, review_queue_handoffs, proposal_readiness_scores, venture_conflicts, capture_strategies, opportunity_expansions, research_revenue_links, submission_readiness_artifacts) + **8 new services** in backend/src/deepResearch/ (pursuitActivation, reviewQueueBridge, proposalReadiness, opportunityExpansion, ventureConflict, researchRevenue, captureStrategy, submissionReadiness) + **2 new frontend components** (StrategicWorkspaceTabs.jsx with 8 tabs; OpportunityGraphCanvas.jsx — vanilla SVG force-directed simulation, NO graph library, ~200 lines total). Pursuit activation supports 11 source kinds (cluster / pattern / agency / technology / opportunity_group / ecosystem / venture / research_run / recommendation / intervention / manual) and produces a fully-loaded PursuitWorkspace + audit row + persisted justification + acceleration suggestions for the top 5 linked opps in one call. Review Queue handoff invokes the existing `actionGenerator.generateOutput` pipeline per linked opp (one draft per opp), idempotent on (pursuit_id, opportunity_id, output_type) re-runs, audits every attempt in `review_queue_handoffs`, and stamps the pursuit's `linkedOutputIds` so the Review Queue surface picks them up naturally. Proposal readiness scores 6 weighted dimensions (staffing 25 + capability 20 + compliance 15 + asset 20 + dependency 10 + acceleration 10 = 100) with explicit blockers/accelerators, expected effort hours, submission risk, and classification (ready / needs_prep / blocked). Capture strategy composes 7 structured fields (evaluator priorities, agency pain points, differentiators, positioning recommendations, incumbent risks, partnership opportunities, reusable language) deterministically — no AI call. Venture conflict uses Jaccard overlap on tokenized title+description+mvpScope vs tool name+description+tags, scores severity with momentum-stage boost, emits differentiation hints (SMB-vs-enterprise, open-source angle, etc). Research-to-revenue maps signal-channel vocabulary to procurement opportunities + agency targets + implementation_demand. Submission Readiness is FOUNDATIONS ONLY per spec (asset model + 5-item default template + status workflow) — full RFP/document/compliance engine lands in Phase 9. Force-directed graph renders only when a center node is picked; pan/zoom/click-to-recenter; opportunity node clicks open the evidence drawer.
  - Date: 2026-05-15
  - Verification: 29 new pure-logic tests in `backend/tests/deepResearch/phase8.test.js` covering all 8 services (pursuitActivation anchor mapping, reviewQueueBridge type whitelist, proposalReadiness clamp/classify/blockers/effort, ventureConflict jaccard/severity/differentiation hints, researchRevenue demand/strength scoring, captureStrategy derivation helpers, submissionReadiness whitelists + template); full Jest suite **1931/1931 tests pass** (+29 net new on top of Phase 7.6's 1902). Zero regressions across all 180 suites. Mid-build eslint fix: ActionIntelligencePage's local `graph` and PursuitWorkspacePage's local `readiness` collided with the new Phase 8 state hooks — renamed to `graphSummary` + `readinessSummary` in commit `793a48f`. Migration ran clean on prod (`migrated 0.362s`); all 8 tables verified. Full end-to-end Phase 8 lifecycle validated on prod: activated pursuit #3 from agency "Dallas Area Rapid Transit (dart)" via `pursuitActivation.activate({sourceKind:"agency"})` → result attached 25 opportunities + 15 acceleration suggestions; `proposalReadiness.scorePursuit(3)` → composite 55.03 / classification needs_prep / 1 blocker; `captureStrategy.buildForPursuit(3)` → 2 evaluator priorities + 1 differentiator + 0 incumbent risks (DART corpus has no dominant tools in the registry); `submissionReadiness.applyDefaultTemplate(3)` → 5 required artifacts seeded. 10 screenshots captured from live prod across Action Intelligence (with CTAs + graph), MyOpps strategic mode (3 tabs), and the pursuit workspace (5 panels) — `docs/deep-research-assets/p8-*.png`. Walkthrough: `docs/deep-research-phase-8-walkthrough.html` (governance contract block, lifecycle section for each of the 8 services, 10 screen sections, schema + routes tables, validation report, known limitations including the eslint fix, recommended Phase 9).
  - Notes: Zero new opportunity-storage tables — reuses Phase 1-7 data throughout. Force-directed graph is vanilla SVG with embedded n-body / spring simulation (~70 lines of math) running for 180 bounded iterations then stopping — no d3 or sigma dependency. Pursuit activation is one-shot per source: re-activating creates a fresh pursuit rather than mutating an existing one. Generate Drafts is sequential (one actionGenerator call per linked opp); a 25-opp pursuit takes ~10 min. Workspace tab state is purely client-side — refresh returns to the Opportunities tab by design. Recommended Phase 9 #1: full Submission Readiness Engine (RFP attachment locker / document vault / compliance matrix generator / package assembler — plan file `staged-spinning-parasol.md` already drafted). #2: semantic conflict detection via embedding column on `ai_tools` + cosine similarity. #3: pursuit-aware proposal context injection — extend `actionGenerator` to accept optional pursuitContext with positioning + capture strategy. #4: parallel draft generation (concurrency 3-4 pool) for large pursuits. #5: pursuit outcomes loop — feed win/lost outcomes back to capture strategy to close the strategic-learning loop.

## Deep Research Intelligence Engine — Phase 7.6 (competing-tools awareness)

- [x] Phase 7.6 — closes the long-standing gap where Deep Research reasoned from opportunity volume alone but never asked "what AI tools are already shipping?". The existing `AiTool` registry (Cursor, Databricks, DataRobot, LangChain, Automation Anywhere, etc., each with momentum scores + trend direction + 7-day mention counts + vendor + category) is now mined during every synthesis run and grounded into the strategic narrative. New `backend/src/deepResearch/competingTools.service.js`: deterministic tokenizer (merges user search-term tokens with top-frequency tokens from opportunity titles, drops stopwords + sub-3-char tokens) → single SQL OR-of-ILIKEs against `ai_tools` (candidate cap 200) → relevance scorer (name hits +3, description hits +1, tag/industry/category hits +1, plus composite_momentum_score/20, trending_score/30, mention_count_7d/10 bonuses; zero-match tools discarded) → cap-12 ranked list + deterministic saturation classifier (empty / emerging / active / crowded; "crowded" requires 6+ tools AND ≥2 dominant/explosive momentum stages). Wired into `strategySynthesis.service`: system prompt explicitly instructs the AI to ground `market_stage` against the tools list ("if dominant/explosive tools are listed, you cannot call the market 'emerging' without justifying it"); user prompt gains a "## Existing AI Tools in This Space (saturation_signal: …)" section listing each tool with vendor/category/momentum/trend/pricing; sanitized output gains a new `competitive_landscape_summary` field. `deepResearch.service.runPipeline` calls `competingTools.findCompetingTools` BEFORE the AI synthesis call (soft-failure — if it throws, the pipeline continues with `competing_tools=null` and the report renders normally); result is persisted into `report.reportJson.competing_tools` + `report.reportJson.competitive_landscape_summary`. `ventureDecisionEngine.decide` accepts two new optional inputs (`competingToolsSignal`, `competingToolsCount`) and adds two new ordered rules: (1) `signal==='crowded' AND compositeScore<75 → decision='OVERSATURATED'` with rationale that cites the tool count explicitly ("8 active AI tools already serve this space …"); (2) when `signal==='empty'` AND market is emerging, the existing TOO_EARLY rationale gains the explicit tail "No AI tools yet ship in this space — reinforces the 'too early' call." Both `factors.competing_tools_signal` + `factors.competing_tools_count` written to the audit trail. **When `competingToolsSignal` is null (older reports / fallback paths), the engine produces output bit-identical to pre-7.6.** Frontend: new `CompetingToolsSection` component rendered above Build Recommendation on `DeepResearchReportPage` — saturation pill, AI narrative, deterministic rationale (italic), per-tool cards with name/vendor/category/momentum chip/trending+momentum scores/open-source flag/website deep-link.
  - Date: 2026-05-15
  - Verification: 20 new pure-logic tests across 2 files (`competingTools.test.js` 14 tests covering tokenizeTerm, tokensFromContext, buildSearchTokens, scoreTool, classifySaturation, summarizeTool; `ventureDecisionEngine.phase76.test.js` 6 tests covering the two new rules + null-input preservation + active-signal pass-through); full Jest suite **1902/1902 tests pass** (+20 net new on top of Phase 7.5's 1882). Zero regressions across all 179 suites. Validated end-to-end on prod after deploy: `findCompetingTools({searchTerm: "workflow automation healthcare", context})` returned 12 matched tools (top-3 gemini-cli 8.20 / Automation Anywhere 8.15 / awesome-claude-skills 8.13) classified as `active`. Re-ran existing report id 4 ("Reporting") via `deepResearch.reRunReport`: result `status: success, has_competing_tools: true, competing_tools_count: 12, saturation_signal: active`, and the AI produced a real competitive-landscape narrative explicitly naming Databricks + DataRobot. 3 screenshots captured from live prod report page (`docs/deep-research-assets/p76-*.png`): full report with new section in context, Competitive Landscape close-up, Build Recommendation in context. Walkthrough: `docs/deep-research-phase-7-6-walkthrough.html` (architecture ASCII diagram, mechanics section per piece, 3 screen sections, validation report, known limitations, Phase 8 recommendations).
  - Notes: Zero new database tables — reuses `ai_tools` for source data and `deep_research_reports.report_json` for persistence. Soft-failure design means a missing/empty AiTool table doesn't break Deep Research. Reports created pre-7.6 have `competing_tools=null` and the UI section renders only when the field exists. The decision-rule escalation fires at the per-venture level (not at the report level) — by design, the report describes the landscape and the venture decides what Colaberry should do. Old reports do NOT auto-rebuild on deploy (intentional — avoids burning unexpected AI tokens); they pick up the new field the next time the user clicks "Re-run". Recommended Phase 8 #1: semantic tool matching via embeddings (add embedding column to ai_tools, cosine similarity in addition to ILIKE so "RAG" catches "retrieval-augmented generation" descriptions). #2: tool-vs-venture overlap detection — when a generated VentureIdea closely matches an existing tool by name + description, flag the conflict before commissioning a build. #3: carry-over from 7.5 — in-page Strategic Workspace Tabs.

## Deep Research Intelligence Engine — Phase 7.5 (contextual opportunity integration)

- [x] Phase 7.5 — bridges Deep Research (strategic intelligence overlay) and My Opportunities (operational execution surface). Strategic context now flows through URL query params; the backend resolves it to an opportunity-id set and uses it as an additive filter on the existing My Opportunities pipeline. **Zero new tables**, **zero parallel UI**, **byte-for-byte backward compatible** when no context param is present. New `backend/src/deepResearch/deepResearchContext.service.js` (single resolver mapping 10 insight kinds — deepResearch / cluster / pattern / venture / recommendation / intervention / pursuit / researchRun / agency / technology — to opportunity-id sets + channel summary + disclosed-value rollup + linked pursuits + return-to-source link; reuses Phase 7 traceability + OpportunityClassification.cluster_id + DeepResearchReport.reportJson + OpportunityRelationship.opportunityIds). Extended `backend/src/oied/myOpportunities.service.listMyOpportunities` to accept an additive `restrictToOpportunityIds` param (null = unchanged, empty array = short-circuit to no-results, non-empty = adds `where.id IN (...)` and drops the $1k value floor since context-narrowed opps are relevant by definition). Extended `backend/src/oied/oied.controller.listMy` to detect context query params (via `pickContextFromQuery`), resolve via `deepResearchContext.getContextSummary`, thread into the service call, and echo the resolved `strategicContext` back on `pagination.strategicContext` so the banner needs no second roundtrip in the common path. New routes `GET /api/v1/deep-research/context/:kind/:id` (banner read) and `GET /api/v1/deep-research/context/opportunity/:id/trace` (per-row evidence). Frontend: new `StrategicContextBanner` component (kind label + classification chip + insight title + channel breakdown chips + disclosed-value rollup + return-to-source link + Open-Pursuit-or-Create-Pursuit CTA + "No auto-submission" footer); new `OpportunityTraceabilityPanel` component (lazy-loaded "Why this opportunity appears" expandable per row, fetches Phase 7 traceability rows pinned with active-context insight first); `MyOpportunitiesPage` extended to detect 10 query params via `detectStrategicContext`, fetch the summary, render the banner above the existing search/filter bar, render traceability panels inside each `OppRow` when context is active, and offer a one-click "Create Pursuit Workspace" CTA seeded with linked opps; "Strategic Context Mode" chip in the H1. New `myOpportunitiesContextUrl(kind, id)` helper in `deepResearchService.js` + `CONTEXT_PARAM_MAP` constant. Open-in-My-Opportunities CTAs added on: Action Intelligence cluster cards, Action Intelligence recurring-agency rows, Cluster Drilldown header (next to Create-Pursuit), Pursuit Workspace header, Research Run focus panel.
  - Date: 2026-05-15
  - Verification: 8 new pure-logic tests in `backend/tests/deepResearch/deepResearchContext.test.js`; full Jest suite 177 suites / **1882 tests pass** (+8 net new on top of Phase 7's 1874). No regressions in any of the 177 suites — `listMyOpportunities` behavior is byte-for-byte unchanged when `restrictToOpportunityIds` is null. Validated end-to-end on prod after deploy: agency context "Dallas Area Rapid Transit (dart)" → resolver returns 25 opportunities (channel breakdown `{bonfire: 25}`, disclosed value $2,905,000); research-run context (id 1 "AI for federal contracts") → 19 opportunities (`{ai_news: 1, ai_job: 18}`); pursuit context (id 1) → resolves correctly; end-to-end `listMyOpportunities({restrictToOpportunityIds: 25-id-set})` returned 25 total / 10 rows on page 1 with all titles from the correct agency. 9 screenshots captured from live prod (`docs/deep-research-assets/p75-*.png`): action-intelligence with Open-in-My-Opps CTAs, agency-context banner, expanded traceability panel, research-run-context banner, pursuit-context banner, default-mode (regression check), cluster CTA, pursuit CTA. Walkthrough: `docs/deep-research-phase-7-5-walkthrough.html` (architecture diagram with ASCII flow, 10-feature table, 10-kind query-state table, 9 screen sections, performance section, known limitations, recommended Phase 8).
  - Notes: Mid-build fix — `MyOpportunitiesPage` had an `eslint-disable react-hooks/exhaustive-deps` directive for a rule not loaded in the prod CRA config (same pattern that broke Phase 7's `ResearchRunsPage`); removed and added `strategicContext` to the effect deps array properly. Strategic mode is detected purely from URL query params — refreshing, paginating, sorting, and filtering preserve the context naturally because the query string survives. Resolver is structured to support multi-context AND-combination as a future extension without schema changes (first matching param wins in v1). Some Phase 6/7 seeded clusters resolve to 0 opportunities because `opportunity_classifications.cluster_id` is null on most prod rows — this is a data state issue, not a Phase 7.5 bug; real classified clusters resolve normally. Recommended Phase 8 #1: in-page Strategic Workspace Tabs (Opportunities / Research / Relationships / Signals / Assets / Pursuits) scoped to the active context. #2: pursuit-to-Review-Queue handoff (pre-fill actionGenerator). #3: multi-context combination (AND across multiple dimensions).

## Deep Research Intelligence Engine — Phase 7 (traceability + opportunity action intelligence)

- [x] Phase 7 — closes the operational trust gap. Every strategic insight (venture / cluster / pattern / recommendation / intervention / pursuit / research run) now links back to its underlying opportunities with a deterministic, auditable rationale. New `/admin/deep-research/action-intelligence` dashboard surfaces the evidence-backed top picks; per-cluster drilldowns explain WHY a cluster matters; pursuit workspaces operationalize strategy into proposal work — **without auto-submitting anything**. 10 new deterministic services in `backend/src/deepResearch/`: `opportunityTraceability.service` (8 insight kinds → supporting opp resolvers + persistence in `opportunity_traceability` + reverse lookup), `opportunityRelationships.service` (recurring detection across agencies / technologies / vendors / NAICS / keywords; 50-term tech vocab; min-occurrence threshold), `opportunityGraph.service` (entity ↔ entity edges from recurring relationships + cluster classifications + report sources; neighborhood read with title hydration), `clusterDrilldown.service` (per-cluster cached themes / agencies / tech / vendors / NAICS / strategic-phrase aggregation), `opportunityJustification.service` (deterministic headline + narrative + factor weights + confidence from traceability — no AI dependency), `customResearchRun.service` (tokenized AND-of-tokens search across Opportunity table; save / pin / rerun / per-run history with delta comparison; preview-mode dry-runs), `proposalAcceleration.service` (mines approved OpportunityOutputs + OrganizationProfile services + NAICS/agency history into reusable proposal components; suggest-for-opportunity scoring), `pursuitWorkspace.service` (anchor-kind venture/cluster/pattern/custom/opportunity → composed pursuit view with linked opps + asset suggestions + staffing heuristic + append-only notes + status workflow), `actionIntelligence.service` (top-level read aggregator: top clusters + strongest evidence-backed opps by aggregate traceability × mean relevance + acceleration candidates + recurring agencies + graph summary + recent runs + pursuit readiness). Migration `20260515000004` creates 8 tables: `opportunity_traceability`, `custom_research_runs`, `pursuit_workspaces`, `opportunity_relationships`, `justification_records`, `opportunity_graph_edges`, `proposal_acceleration_assets`, `cluster_drilldowns`. 24 new admin-only routes under `/api/v1/deep-research/*` (action-intelligence + evidence/:kind/:id + justification/:kind/:id + clusters/:id/drilldown + research-runs/* + graph/* + relationships/* + acceleration/* + pursuits/*). Frontend: 4 new pages (`ActionIntelligencePage`, `ClusterDrilldownPage`, `PursuitWorkspacePage`, `ResearchRunsPage`); `ActionVisuals` component module with `EvidenceDrawer` overlay (re-usable everywhere) + `JustificationCard` + channel/relevance badges + pursuit status pill; App.jsx routes + 2 new Sidebar links (🎯 Action Intelligence, 🔎 Strategic Search Runs); 24 new API client functions in `deepResearchService.js`.
  - Date: 2026-05-15
  - Verification: 46 new pure-logic tests across 7 files in `backend/tests/deepResearch/`; full Jest suite 176 suites / **1874 tests pass** (+46 net new on top of Phase 6's 1828). Migration ran clean on prod (`migrated 0.409s`); all 8 tables verified via `information_schema.tables`. Validated end-to-end on prod: `refreshRelationships` produced 1,259 recurring relationships across 5,000 active opportunities; `refreshGraph` built an 8,641-edge graph across 5 entity kinds (technology 378 + keyword 7,991 + agency 240 + naics 12 + vendor 20); `refreshAssets` mined 3 past-win acceleration assets from approved OpportunityOutputs; `getActionIntelligence` read returned 6 top actionable clusters + 10 proposal acceleration candidates + 10 recurring agencies + the graph summary; 5 cluster drilldowns pre-cached; 2 sample research runs created (tokenize → query → persist with history); 1 sample pursuit workspace created anchored to a cluster with persisted justification. 11 screenshots captured from live prod (`docs/deep-research-assets/p7-*.png`). Walkthrough: `docs/deep-research-phase-7-walkthrough.html` (10 feature blocks, 10 screen sections, governance contract listing what engines are/aren't allowed to do, schema + routes tables, validation report, recommended Phase 8).
  - Notes: Governance contract is enforced in code — no Phase 7 service imports from a lifecycle-write path or scoring-write engine; every recommendation links to its evidence; every justification is composed deterministically with no AI dependency. Mid-build fixes: (1) clusterDrilldown test expected "ai" in topTerms output but the 3+ char regex drops 2-char tokens — updated test to use "rag" instead; (2) ResearchRunsPage had an eslint-disable comment for a rule that isn't loaded in the prod CRA config — removed the comment so the prod frontend build succeeds. Traceability is lazy by design: first hit on an insight derives links on-the-fly (worst case +200ms); `POST /evidence/:kind/:id/rebuild` persists them. Recommended Phase 8 #1: pursuit-to-Review-Queue handoff (pre-fill `actionGenerator` from a pursuit's linked opps + suggested assets + positioning); #2: force-directed graph viewer (data layer ready, ~200 lines of canvas); #3: Submission Readiness Engine (separate track — `staged-spinning-parasol.md` plan already drafted).

## Deep Research Intelligence Engine — Phase 6 (adaptive strategic operations)

- [x] Phase 6 — adaptive strategic operations: closes the loop by adding automated safe refresh + executive recommendations + operational/venture health intelligence + forecast-vs-reality measurement + a single Executive Planning Workspace UI. **Recommend-only, deterministic, human-in-the-loop — the engines never move lifecycle states, never mutate scoring, never auto-execute anything.** 10 new deterministic services in `backend/src/deepResearch/`: `adaptiveRefresh.service` (cron-driven full/portfolio/observatory/decay refreshes, opt-in via `DEEP_RESEARCH_ADAPTIVE_REFRESH_ENABLED=true`, default cron `30 3 * * *`, per-step error isolation, idempotent retries), `refreshHistory.service` (startRun/recordStep/completeRun lifecycle helper persisting to `adaptive_refresh_runs` + `refresh_history`), `executiveIntervention.service` (6 recommendation types: hire / queue_pressure / redistribute / venture_pause / sequencing / ecosystem_diversification, idempotent on (type, title) pair, refreshes metrics on existing pending rows), `strategicRecommendation.service` (5 portfolio-level types: portfolio_rebalance / execution_pacing / infrastructure_consolidation / venture_acceleration / monitoring), `adaptiveAnalytics.service` (read-only composite payload joining capacity / portfolio scores / ecosystem concentration / transition flow / readiness distribution / refresh health / dependency load), `ventureHealth.service` (6 health classes per venture — healthy / strengthening / at_risk / overloaded / stagnating / declining — from composite signals: state + readiness + confidence delta + stall days + trajectory + overlap count; 0-100 informational score + suggested intervention; append-only history table), `forecastReality.service` (4 forecast kinds: readiness / timeline / staffing / roi — classifies predicted vs actual as accurate / optimistic / pessimistic / unknown; MEASURE-ONLY by design, never re-tunes weights), `operationalDrift.service` (5 detectors distinct from Phase 5 strategic drift: queue_drift / execution_slowdown / staffing_imbalance / dependency_accumulation / execution_bottleneck), `dependencyReview.service` (workflow log on Phase 5 dependency_edges with 5 actions: approve→confirmed / reject→dismissed / annotate / assign_owner / resolve), `planningWorkspace.service` (read-only aggregator that powers the single dashboard payload). Migration `20260515000003` creates 8 tables: `adaptive_refresh_runs`, `refresh_history`, `intervention_recommendations`, `strategic_recommendations`, `venture_health_history`, `forecast_accuracy`, `operational_drift`, `dependency_reviews`. 18 new admin-only routes under `/api/v1/deep-research/planning/*` (GET planning workspace + analytics + forecast-accuracy + refresh-runs; POST refresh; GET + acknowledge/dismiss interventions / strategic-recommendations / operational-drift / venture-health; GET + POST actions on dependency-review). Cron scheduler wired in `server.js` (opt-in). Frontend: `ExecutivePlanningPage` at `/admin/deep-research/planning` (single workspace: top KPI strip + venture health bucket strip with per-venture table + interventions + strategic recommendations + forecast accuracy stacked bars + operational drift + dependency review workflow with inline action buttons + continuous refresh history table); `PlanningVisuals` component module (HealthBadge / AccuracyBadge / RefreshStatusBadge / SeverityPill / RecommendationRow / HealthBucketStrip / AccuracyBar / RefreshRunRow / VentureHealthRow / DependencyReviewRow); App.jsx route + Sidebar "🛰 Executive Planning" link. Frontend service module extended with 20 new API client functions.
  - Date: 2026-05-15
  - Verification: 43 new pure-logic tests across 7 files in `backend/tests/deepResearch/`; full Jest suite 169 suites / **1828 tests pass** (+43 net new on top of Phase 5's 1785). `tsc --noEmit` not run (project is JavaScript, not TypeScript). Migration ran clean on prod (`migrated 0.374s`); all 8 Phase 6 tables verified via `information_schema.tables`. Adaptive refresh validated end-to-end on prod: first run surfaced an `EcosystemMetric` column-name bug (`captured_at` vs `computed_at`); fixed in commit `70897fd` and re-validated — second full refresh returned `status: success errors: []`. Planning workspace endpoint returns valid composite payload: 4 interventions, 2 strategic recs, 3 venture health rows (all healthy), 1 operational drift, 3 open dependency edges, 2 refresh runs in history. 10 screenshots captured from live prod planning page (`docs/deep-research-assets/p6-*.png`). Walkthrough: `docs/deep-research-phase-6-walkthrough.html` (10 feature blocks, 10 screen sections, governance contract block listing what engines are/aren't allowed to do, schema + routes tables, files-changed inventory, recommended Phase 7).
  - Notes: Governance contract is enforced in code, not just documented — every Phase 6 service is read-then-recommend; no Phase 6 service imports from any lifecycle-write path or scoring-write engine. Cron defaults OFF in prod (must set `DEEP_RESEARCH_ADAPTIVE_REFRESH_ENABLED=true` to arm; cron expression overridable via `DEEP_RESEARCH_ADAPTIVE_REFRESH_CRON`). Idempotency on recommendation tables is by (type, title) tuple so the row identity stays stable when underlying numbers change — title encodes magnitude, so a deficit going from 2 → 3 intentionally creates a fresh row. Health classification thresholds are env-overridable (`DEEP_RESEARCH_STALL_DAYS` default 30). Forecast accuracy uses lifecycle progression as a proxy for "actual" until external ground truth (real revenue, real timeline data) is wired in — explicitly classified `unknown` when proxy is meaningless. Recommended Phase 7 #1: integrate top pending interventions into the existing morning briefing email; #2: health-trend sparklines once `venture_health_history` has ≥30 days; #3: Anthropic fallback provider for Phase 2/3 AI steps.

## Deep Research Intelligence Engine — Phase 5 (temporal + ecosystem intelligence)

- [x] Phase 5 — temporal reasoning across the platform: trend movement, venture trajectories, ecosystem evolution, decision-accuracy measurement, directional dependencies + critical-path analysis, predictive capacity forecasting, strategic-drift detection. New deterministic services in `backend/src/deepResearch/`: `temporalIntelligence.service` (generic key/value time-series + 6-class movement classification accelerating/strengthening/stable/weakening/decelerating/insufficient_data, env-overridable thresholds), `historicalPortfolio.service` (assembles capacity timeline + venture velocity + readiness aging + confidence trend + portfolio ranking trend from existing time-series tables), `ecosystemEvolution.service` (per-ecosystem health/maturity/momentum 0-100 scores from venture_templates as keys; classifies emerging/accelerating/saturated/declining/dying/converging; lifecycle-progress lookup table + mean-confidence-delta-driven momentum), `ventureTrajectory.service` (per-venture trajectory classification strengthening/accelerating/stable/weakening/stagnating/unknown from confidence delta + score delta + lifecycle velocity over a window; stagnating fires only when zero movement AND zero transitions), `decisionAccuracy.service` (MEASURE-ONLY — never modifies scoring; buckets ventures by decision type, classifies outcome progressed/stalled/reversed with archived always reversed regardless of lifecycle index, computes accuracy with decision-class-specific correctness criteria), `ventureDependency.service` upgraded (added directional `dependency_edges` with proposeFromOverlap heuristic — more-advanced venture is the blocker — autoProposeDirectionalEdges, cycle-safe DFS computeCriticalPath, identifyBottlenecksOnGraph for top blockers + most-blocked), `predictiveCapacity.service` (walks the Phase 4 capacity plan forward to 30/90/180-day horizons under optimistic/realistic/conservative scenarios with slippage and arrival multipliers; projects staffing pressure, queue pressure, concurrency demand, bottleneck risks), `strategicDrift.service` (5 deterministic checks ecosystem_overcommitment/risk_concentration/execution_overload/portfolio_imbalance/strategy_drift → drift_alerts as RECOMMENDATIONS only, dedupes pending alerts of same type), `signalHistory.service` (snapshots portfolio-level signals into a generic time-series table for sparklines), `observatoryIntelligence.service` (top-level orchestrator: `refreshObservatory` runs all engines with per-step error isolation; `getObservatoryDashboard` assembles the read view). Migration `20260515000002` creates 8 tables (`temporal_snapshots`, `ecosystem_metrics`, `venture_trajectories`, `decision_accuracy`, `predictive_capacity`, `drift_alerts`, `signal_history`, `dependency_edges`). Routes at `/api/v1/deep-research/observatory[/{refresh,ecosystems,trajectories,decision-accuracy,predictive-capacity,drift,dependencies,history,signals}]` + drift acknowledge/dismiss + dependency-edge status patch. Frontend: `EcosystemObservatoryPage` (`/admin/deep-research/observatory` — temporal movements, ecosystem evolution cards with 3-bar health/maturity/momentum gauges, venture trajectories table, decision accuracy table, predictive capacity 3-horizon grid, directional dependencies + critical path, drift alert rows, historical signal sparklines, historical analytics), `TemporalVisuals` (TrajectoryBadge/EcosystemBadge/MovementBadge/Sparkline/DriftAlertRow/EcosystemBars), sidebar Ecosystem Observatory link. Human-in-the-loop preserved end-to-end — every drift alert + directional edge is a recommendation; decision-accuracy is measure-only by design.
  - Date: 2026-05-15
  - Verification: ~50 new tests across 7 test files (`backend/tests/deepResearch/`); full suite 162 suites / 1785 tests pass. Migration ran clean on prod (`migrated 0.374s`); all 8 tables verified. Observatory orchestrator validated end-to-end on prod: `refresh ok=true errors=0`, 8 signal-history points captured, 4 ecosystems classified (all "converging" — correct: seeded ventures span multiple templates), 3 venture trajectories (all "stable" — correct for fresh history; classifier returns insufficient_data gracefully when needed), 3 directional dependency edges auto-proposed from existing overlap pairs + lifecycle precedence, 9 predictive-capacity forecasts (3 horizons × 3 scenarios), 2 drift alerts emitted correctly (ecosystem_overcommitment at 100 + execution_overload at 100 — the Phase 4 over-capacity state correctly surfaces as drift). 4 screenshots (`docs/deep-research-assets/p5-*.png`). Walkthrough: `docs/deep-research-phase-5-walkthrough.html`.
  - Notes: Mid-build fix — decision-accuracy `classifyOutcome` was returning `progressed` for ventures archived after evaluating because archived sits at the end of the lifecycle index but moving to it is failure; reordered checks so archived → reversed always wins. Most temporal engines need accumulated history to be meaningful — the classifiers correctly return `insufficient_data` / `stable` / `unknown` on a fresh system rather than misleading numbers, so they're useful from day 1 with honest "not enough data yet" output and become rich as snapshots accumulate. Phase 5 retains every Phase 1-4 system unchanged. Recommended Phase 6 step #1: schedule daily Observatory + Portfolio refreshes (deterministic + cheap, safe to run unattended) — that's how the temporal engines accumulate the history they need.

## Deep Research Intelligence Engine — Phase 4 (portfolio + capacity intelligence)

- [x] Phase 4 — portfolio-level deterministic engines. Evolves the platform from "can this venture succeed?" to "should this venture consume organizational capacity right now?" — capacity, prioritization, dependencies, ROI, sequencing, templates, confidence decay across the whole venture set. New services in `backend/src/deepResearch/` (all deterministic): `resourceCapacity.service` (aggregates staffing + infra demand across active ventures vs configurable supply — `DEEP_RESEARCH_ORG_HEADCOUNT`/`DEEP_RESEARCH_ORG_ROLES`/`DEEP_RESEARCH_ORG_INFRA_CAPACITY` — produces pressure scores 0-100, concurrency limit, role-level deficits, bottleneck list), `portfolioPrioritization.service` (7-factor ranking with defined weight model: composite/execution_readiness/decision/roi/resource_pressure_penalty/infrastructure_reuse_bonus/timing → portfolio_score + sequencing recommendation execute_now/queue/defer/monitor/pass, fully traceable rationale), `sharedInfrastructure.service` (per-pair Jaccard overlap on component fingerprints — infra items + stack + AI components + architecture signature tokens — detects shared build opportunities across 3+ ventures), `ventureDependency.service` (3 edge types: shared_architecture/shared_staffing/shared_ai_provider with cascade-risk scores + sequencing constraints), `portfolioForecasting.service` (regex pricing parser → per-customer MRR → 3 scenarios optimistic/realistic/conservative with 1.5×/1.0×/0.5× MRR and 0.85×/1.0×/1.2× cost multipliers; staffing cost from headcount × weekly rate × MVP weeks; infra cost from items × monthly rate × 12; break-even months + 12mo ROI; rolled-up portfolio totals), `executionCapacityPlanner.service` (priority-sorted slot-packing across quarters, hiring recommendations from role deficits), `ventureTemplate.service` (6 built-in keyword patterns — AI assistant/workflow automation/AI education/gov intelligence/AI orchestration/AI observability — aggregates per-template MVP scaffolds + GTM playbooks with frequency counts), `confidenceDecay.service` (age + stuck-time + decision-pressure decay; emits reassess/reprioritize/accelerate/monitor RECOMMENDATIONS only — never auto-transitions, dedupes pending recommendations of same type), `portfolioIntelligence.service` (top-level orchestrator: `refreshPortfolio` runs every engine in dependency order with per-step error isolation; `getPortfolioDashboard` assembles the read view; `computeOperationalMetrics` derives venture velocity / cycle-time per stage / decision quality / readiness aging). Migration `20260515000001` creates 8 tables (`portfolio_scores`, `resource_capacity_snapshots`, `venture_dependencies`, `infrastructure_overlap`, `roi_forecasts`, `confidence_history`, `reassessment_events`, `venture_templates`). Routes at `/api/v1/deep-research/portfolio[/{refresh,capacity,ranking,forecasts,overlaps,dependencies,plan,templates,recommendations,operations}]` + `recommendations/:id/{acknowledge,dismiss}`. Frontend: `PortfolioDashboardPage` (`/admin/deep-research/portfolio` — venture portfolio command center: stats + capacity pressure + ROI scenarios + ranking table + quarter plan + dependency edges + shared infra + templates + reassessment recommendations + operational metrics), `PortfolioVisuals` (PressureGauge, StatTile, SequencingBadge, RoiScenarios, BottleneckList, DependencyEdgeList, QuarterPlanGrid, RecommendationRow), sidebar Portfolio Intelligence link. Human-in-the-loop preserved throughout — every recommendation surfaces for human action; nothing auto-applies.
  - Date: 2026-05-15
  - Verification: ~50 new tests across 8 test files (`backend/tests/deepResearch/`); full suite 155 suites / 1739 tests pass. Migration ran clean on prod (`migrated 0.438s`); all 8 tables verified. Portfolio orchestrator validated end-to-end on prod via the seeded sample: `refresh ok=true errors=0`, capacity pressure 100 (correctly identified 2 BUILD_NOW vs concurrency 1 cannot all run in parallel), 4 bottlenecks identified (tech lead + product owner deficits + both pressure overloads), 3 ventures ranked with all 3 correctly downgraded to `queue` (the 7-factor model's `resource_pressure_penalty` did its job — the engine wouldn't recommend `execute_now` when the org is over capacity), 3 overlap pairs + 4 shared opportunities, 6 dependency edges + 3 sequencing constraints, realistic portfolio MRR $330k/mo, capacity plan schedulable 3/3, 4 templates detected, decay evaluated 3 ventures. 4 screenshots (`docs/deep-research-assets/p4-*.png`). Walkthrough: `docs/deep-research-phase-4-walkthrough.html`.
  - Notes: Mid-build fix — Sequelize toJSON returns camelCase attribute names but the dashboard initially read snake_case DB column names → pressures rendered as 0; switched to camelCase + enriched `getLatestRanking` with a venture-title join. Per the user's continued architectural guidance, every Phase 4 engine is deterministic; the only AI dependency is via Phase 2/3 (consumed as inputs to the deterministic engines). Pricing parser is regex-based — handles common formats; ventures whose monetization can't be parsed get zero-MRR conservatively rather than fake numbers. Default org supply is a small-team heuristic, env-overridable. Templates are keyword-based for transparency. Decay never auto-acts — recommendations only. Recommended Phase 5 step #1: schedule the portfolio refresh + decay-pass as opt-in daily crons (deterministic + cheap, safe to run unattended).

## Deep Research Intelligence Engine — Phase 3 (execution intelligence)

- [x] Phase 3 — venture lifecycle + execution engines + real architect client. Operationalizes the intelligence layer: a venture idea now has an 11-state lifecycle, a deterministic execution readiness assessment, an explainable build-vs-monitor decision, an AI MVP plan + launch strategy, a deployment readiness level, and a place in a priority-sorted execution queue. New services in `backend/src/deepResearch/`: `ventureLifecycle.service` (11-state machine — explicit transition graph, validation, audit trail `venture_lifecycle_events`, keeps denormalized state in sync), `projectArchitectClient.service` (real production-shaped HTTP client for Agent Foundry — AbortController timeouts, exponential-backoff retry, resumable polling, artifact fetch, typed error classification; targets configurable `AGENT_FOUNDRY_API_URL` + `AGENT_FOUNDRY_API_KEY`; throws `NOT_CONFIGURED` until set), `executionReadiness.service` (deterministic — 4 sub-scores technical_complexity/ai_dependency_risk/market_readiness/operational_readiness, weighted composite, MVP timeline estimate, staffing + infrastructure profiles), `ventureDecisionEngine.service` (deterministic ordered-rule engine — BUILD_NOW/BUILD_SOON/MONITOR/TOO_EARLY/OVERSATURATED/HIGH_RISK/NEEDS_VALIDATION, first-match-wins, fully traceable rationale + factor breakdown), `deploymentReadiness.service` (deterministic — prototype/mvp/production/enterprise from 5 signals, compliance exposure gates the top levels), `mvpPlanning.service` (AI hybrid — plan content via aiProvider + deterministic timeline from executionReadiness), `launchStrategy.service` (AI — ICP/GTM/landing/outreach/channels/pricing/pilot/enterprise/gov), `executionQueue.service` (orchestrator — `assessVenture` runs the deterministic engines + persists + refreshes the queue row, `planMvp` runs the AI engines + deployment readiness with graceful per-step degradation, `getVentureExecution`/`listQueue`/`getPipeline` reads). `projectArchitectBridge` upgraded — `processJob` dispatches to a real-architect path (createJob + resumable polling, provider='agent_foundry') vs the foundation walk, chosen by `projectArchitectClient.isConfigured()`. Migration `20260514000003` creates 6 tables (`venture_lifecycle_events`, `execution_readiness`, `mvp_plans`, `launch_strategies`, `deployment_readiness`, `execution_queue_items`) + `lifecycle_state`/`lifecycle_owner` on venture_ideas + `external_job_id`/`provider` on project_generation_jobs. Routes: `/ventures/:id/{lifecycle,owner,assess,mvp-plan,execution}` + `/execution/{queue,pipeline}`. Frontend: `ExecutionDashboardPage` (`/admin/deep-research/execution` — venture operations center: summary stats + pipeline flow + priority-sorted execution queue), `VentureExecutionPanel` (lazy collapsible per-venture execution intelligence on the report page — lifecycle tracker + transition controls + Assess/Plan-MVP actions + readiness gauge + decision + MVP plan + launch strategy + deployment readiness), `ExecutionVisuals` (readiness gauge, lifecycle tracker, decision/deployment/lifecycle badges, sub-score bars), sidebar Execution Intelligence link. Human-in-the-loop preserved throughout — nothing auto-executes, auto-builds, auto-deploys, or auto-contacts.
  - Date: 2026-05-14
  - Verification: ~50 new tests across 8 test files (`backend/tests/deepResearch/`); full suite 147 suites / 1691 tests pass. Migration ran clean on prod (`migrated 0.468s`); all 6 tables + columns verified. The seeder (updated to transition the sample ventures through lifecycle stages + run the real `assessVenture`) validated the deterministic engines live on prod: 3 venture ideas moved discovered→researching→evaluating→approved, assessed to execution readiness 78–81, all decided `BUILD_NOW`, landed in the execution queue with priority + 1 correct blocker each ("Requirements not yet generated"); `getPipeline` shows 3 stages populated; `getVentureExecution` assembles lifecycle + decision + readiness; `projectArchitectClient.isConfigured()` correctly false. Routes mounted (verified — 429 from rate limiter on rapid probes, not 404). 4 screenshots (`docs/deep-research-assets/p3-*.png`). Walkthrough: `docs/deep-research-phase-3-walkthrough.html`.
  - Notes: AI engines (mvpPlanning, launchStrategy) not validated against live AI — OpenAI quota still exhausted; they go through the resilient aiProvider layer and are unit-tested incl. failure paths; `planMvp` degrades gracefully (deterministic deployment readiness still runs if both AI engines 429). Agent Foundry endpoint not live — per the user's chosen approach, the client is real + fully tested (mocked fetch) but unconfigured; setting the env vars switches the bridge to the real path with no code change. Folder + route conventions follow the repo. Recommended Phase 4 step #1: wire an Anthropic fallback provider (the aiProvider seam is built) to unblock live validation of all the Phase 2 + 3 AI engines.

## Deep Research Intelligence Engine — Phase 2 (real strategic intelligence)

- [x] Phase 2 — four intelligence engines + AI resiliency + report management. Upgrades the Phase 1 foundation into real strategic intelligence. New services in `backend/src/deepResearch/`: `aiProvider.service` (resilient AI layer — exponential backoff retry with jitter on transient error classes, typed error classification rate_limit/auth/server/network/bad_response/other, per-attempt logging to `ai_provider_logs`, provider abstraction registry with OpenAI wired + Anthropic/Gemini documented stubs; strategySynthesis + ventureIdeaGenerator switched to call it), `crossChannelCorrelation.service` (deterministic — per-channel signal scoring weighted by commercial weight, convergence classification commercial_acceleration/procurement_pull/capital_convergence/talent_convergence/research_only/diffuse/none, correlation_strength as 60/40 breadth/depth blend, volume-weighted acceleration), `marketTiming.service` (deterministic — momentum × maturity quadrant → emerging/acceleration/breakout/mainstream/saturated/declining, timing_score, data-backed confidence), `ventureScoring.service` (deterministic — 8 dimensions commercialization/buildability/market_timing/competition_saturation/technical_feasibility/revenue_potential/gov_alignment/ai_defensibility, module-constant weights summing to 1.0, weighted composite → strong_build/build/watch/pass), `monetizationIntelligence.service` (hybrid — AI content for 6 model types saas/enterprise/government/education/services/marketplace, deterministic fit_score from channel signal mix), `briefingSubscription.service` (CRUD + preview + history for DB-driven scan config). Orchestrator `deepResearch.service` wires all four engines into the pipeline + adds `listReports` (searchable/filterable index), `reRunReport` (versioned re-run — snapshots into `report_versions`, bumps version, re-runs on the same report id), `setReportFlag` (favorite/archive), `getReportVersions`. `projectArchitectBridge` handoff is now venture-aware (strategic context, monetization models, ICP, GTM, competitive landscape). `dailyResearchScheduler` is DB-driven (reads `briefing_subscriptions`, env-topic fallback). Migration `20260514000002` creates 5 tables (`report_versions`, `signal_correlations`, `monetization_models`, `ai_provider_logs`, `briefing_subscriptions`) + 9 columns on the Phase 1 tables. Routes at `/api/v1/deep-research`: `GET /` (index), `POST /:id/rerun`, `PATCH /:id/flags`, `GET /:id/versions`, briefings CRUD + `/briefings/preview` + `/briefings/history`. Frontend: `DeepResearchIndexPage` (intelligence terminal), `BriefingCenterPage`, `IntelVisuals` (confidence gauge, score bars, correlation strength bar, signal heat strip, badges), upgraded `DeepResearchReportPage` (correlation section with recharts acceleration chart, market timing with gauge, monetization models, 8-dimension venture scores, re-run + favorite), sidebar Intelligence section.
  - Date: 2026-05-14
  - Verification: 53 new tests across 6 test files (`backend/tests/deepResearch/`) + Phase 1 test mocks updated for the aiProvider switch; full suite 139 suites / 1630 tests pass. Migration ran clean on prod (`migrated 0.300s`); all 5 tables + 9 columns verified. Routes mounted + auth-gated (`GET /api/v1/deep-research` → 401). The seeder (`scripts/seed-deep-research-sample.js`, updated to run the real engines) validated the deterministic engines live on prod: `commercial_acceleration` convergence at 0.771 strength, `breakout` market stage, venture composite 77.48 → `strong_build` with all 8 score dimensions persisted, 3 monetization models with computed fit scores. Management surfaces validated on prod: `listReports`, `getReport` (Phase 2 shape with correlation + monetization), `setReportFlag` toggle, briefing subscription create/delete, `aiProvider.classifyError`. 5 screenshots (`docs/deep-research-assets/p2-*.png`). Walkthrough: `docs/deep-research-phase-2-walkthrough.html`.
  - Notes: Live AI synthesis still unvalidated end-to-end — OpenAI account quota exhausted (429s). The deterministic engines were validated by running the real engine code on prod; the AI steps + their failure paths are unit-tested. The aiProvider resiliency layer + provider seam are the de-risk: wiring an Anthropic fallback (a localized change in the `PROVIDERS` registry) is the recommended Phase 3 step #1. Folder + route conventions follow the repo. Phase 2 marketStage vocab (emerging/acceleration/breakout/mainstream/saturated/declining) replaces the Phase 1 too_early/emerging/active/saturated — frontend STAGE_META updated accordingly.

## Deep Research Intelligence Engine — Phase 1 (foundation)

- [x] Phase 1 — Deep Research foundation. Transforms cross-channel opportunity data into venture intelligence. New `backend/src/deepResearch/` feature folder with 6 single-responsibility services: `deepResearch.service` (orchestrator — aggregates + normalizes cross-channel opp data into a strategic context, owns the report lifecycle, delegates AI work), `strategySynthesis.service` (AI: executive narrative — exec summary, market stage, opportunity signals, gov alignment, MVP directions, build rec), `ventureIdeaGenerator.service` (AI: venture-idea shortlist — monetization, buildability, revenue band, GTM), `projectArchitectBridge.service` (project_generation_jobs lifecycle — 5-phase walker + progress tracking; the Agent Foundry handoff is one swappable seam `_dispatchToAgentFoundry`, Phase 1 generates a requirements scaffold deterministically), `dailyResearchScheduler.service` (opt-in once-per-day scan, `DEEP_RESEARCH_SCAN_ENABLED` default OFF), `emailBriefing.service` (executive briefing email — conservative delivery, only sends when `DEEP_RESEARCH_BRIEFING_TO` is set). Migration `20260514000001` creates 4 tables (`deep_research_reports`, `venture_ideas`, `project_generation_jobs`, `daily_research_scans`) + models. `AnalysisRun` enum gains `deep_research` + `daily_research_scan`. Routes at `/api/v1/deep-research` (admin-only): `POST /run`, `GET /:id`, `GET /:id/status`, `POST /:id/generate-requirements`, `GET /jobs/:id/status`. Frontend: Deep Research button on My Opportunities (uses current search + filtered results), `DeepResearchReportPage` at `/admin/deep-research/:id` (10-section executive dashboard), `RequirementsProgressModal` (polls job status, phase tracker + progress bar).
  - Date: 2026-05-14
  - Verification: 53 new tests across 6 test files (`backend/tests/deepResearch/`); full suite 133 suites / 1570 tests pass. Migration ran clean on prod (`migrated 0.213s`); all 4 tables verified in `pg_tables`. Route mounted + auth-gated (`GET /api/v1/deep-research/1` → 401 unauthenticated). Sample report seeded (`scripts/seed-deep-research-sample.js` — report id 1, 3 venture ideas, status success). projectArchitectBridge job lifecycle validated end-to-end on prod: createJob → success, 100%, all 5 phases `completed`, requirements scaffold with 13 keys persisted. Screenshots captured (`docs/deep-research-assets/`, 5 PNGs). Walkthrough: `docs/deep-research-phase-1-walkthrough.html`.
  - Notes: Live AI synthesis (`runDeepResearch`) not validated on prod — the OpenAI account quota is exhausted (429s, same issue affecting the research crons). All AI code paths incl. the 429 failure path are unit-tested; the report page / venture ideas / requirements flow were validated against the seeded sample. Mid-build fix: the projectArchitectBridge phase walker mutated the phases JSONB array in place — Sequelize saw the same reference and skipped the column write; fixed with a `clonePhases` deep-copy on every persist. Folder + route conventions follow the repo (`backend/src/deepResearch/`, `/api/v1/deep-research`), not the spec's draft paths (`backend/src/services/`, `/api/deep-research`).

## Research Intelligence Expansion — fixes

- [x] Research missing from the keyword-search per-channel breakdown strip. `ChannelBucketStrip` in `MyOpportunitiesPage.jsx` had a hardcoded `order` array (`private-sector, talent, government, bonfire, capital, freelance, strategic`) that never got `research` added — the backend already built a `research` bucket (its loop runs over all 8 `channelsSvc.listChannels()`), but the frontend filtered it out. Added `'research'` to the order array (before `strategic`).
  - Date: 2026-05-14
  - Verification: user-reported (screenshots — `?q=mcp` breakdown showed News/Talent/Freelance cards but no Research card despite `?channel=research&q=MCP` returning 2 matches). Backend confirmed already correct; one-line frontend order-array fix. Deploy pending.

## Research Intelligence Expansion — Phases 2.4–4 (intervals, tier-2 adapters, semantic search + graph, build briefs)

- [x] Phase 2.4 — per-source ingestion intervals. Migration `20260510000008` adds `ingest_interval_minutes` to `data_sources` (seeds arxiv=120, semantic_scholar=1440, huggingface_papers=360). `DataSource` model gains `ingestIntervalMinutes`. New `ingestion/research.scheduler.js` ticks every 30 min, runs only sources whose interval has elapsed since `last_run_at`. Wired into `server.js` after the main scheduler.
  - Date: 2026-05-09
  - Verification: backend suite passes. Smoke: scheduler ticks, arxiv re-ingested on the 2h cadence on prod.
- [x] Phase 2.5 — tier-2 research adapters. `papersWithCode.adapter.js` (built, then disabled on prod — Meta killed the public API, returns HTML) and `researchBlogRss.adapter.js` (configurable multi-feed RSS — OpenAI/DeepMind/BAIR — via `rss-parser`). Migration `20260510000009` seeds both data_sources rows.
  - Date: 2026-05-09
  - Verification: researchAdapters.test.js extended. Notes: papers_with_code + semantic_scholar both disabled via SQL on prod (dead API / keyless 429); research_blogs live.
- [x] Phase 3 — semantic search + research relationship graph. Migrations `20260510000010` (Opportunity gains `embedding` JSONB + `embedding_model` + `embedded_at` + partial index) and `20260510000011` (`research_relationships` edge table, unique on the edge triple). `ai.client.js` gains `embed()` (text-embedding-3-small, single or batched). New `semanticSearch.service.js` (embeddingText, cosineSimilarity, embedOpportunities, semanticSearch, findSimilar — in-process cosine, no pgvector — prod DB image swap deemed too risky, JSONB approach is forward-compatible). New `researchGraph.service.js` (buildRelationships materializes cross_channel_matches into edges; buildTopicGraph assembles a topic tree). `research.controller.js` + routes: `/research/search`, `/research/opportunities/:id/similar`, `/research/topics`, `/research/authors`, `/research/graph/:topic`, `POST /research/embed`. Embedding cron 6:30 AM, graph-build cron 7:30 AM. `AnalysisRun` enum gains `embedding_generation` + `research_graph_build`.
  - Date: 2026-05-09
  - Verification: semanticSearch.test.js + researchGraph.test.js. Smoke-run on prod: 239 research opps embedded, semantic search returns 195 matches (top: "MEME: Multi-entity & Evolving Memory Evaluation", sim 0.642), graph build upserted 264 edges across 47 research opps.
- [x] Phase 4 — research-to-build brief generator. New `oied/researchBuildBrief.service.js` turns a buildable research paper into a concrete build plan (product_idea, target_customers, suggested_architecture, mvp_scope, proposal_angle, confidence) via gpt-4o-mini, stored on `aiAnalysis.build_brief`. Only briefs papers Phase 2.1 flagged `research_summary.buildable === true`; grounds target_customers in the paper's `cross_channel_matches` demand signal. Idempotent batch (skips already-briefed unless force=true). Daily cron 7:45 AM UTC. `POST /research/build-briefs` admin trigger. Frontend OpportunityCard shows a 🧭 Build brief badge. `AnalysisRun` enum gains `research_build_brief`.
  - Date: 2026-05-14
  - Verification: 10 new tests (researchBuildBrief.test.js). Full suite 127 suites / 1520 tests pass. Prod smoke-run: 9 briefs persisted (11 of 15 had a demand signal, 29 buildable total) with grounded content; 6 opps failed on OpenAI 429 quota — service caught per-opp, marked the run `partial`, persisted the 9 successes (failure-first design working as intended).
  - Notes: No migration — `AnalysisRun.type` is a plain varchar (enum enforced in-model only) and `build_brief` lives in the existing `aiAnalysis` JSONB. The 429s are an OpenAI account billing/quota issue, not a code defect — re-running the batch after quota refresh will pick up the remaining buildable papers (idempotent, skips already-briefed).

## Research Intelligence Expansion — Phase 2 (2.1–2.3: summaries, cross-channel, aggregation)

- [x] Phase 2.1 — AI research summaries. New `oied/researchSummary.service.js` generates a business-oriented summary per research opp (executive_summary, build_recommendation + buildable flag, market_timing one of too_early/emerging/active/saturated, competitive_insight) via gpt-4o-mini, stored on `aiAnalysis.research_summary`. Idempotent batch + single-opp paths, mirrors classification.service. Daily cron 6:45 AM UTC in actionEngine.scheduler. Frontend OpportunityCard ResearchMeta shows market-timing + 🔨 Buildable badges. `AnalysisRun` enum gains `research_summary`.
  - Date: 2026-05-09
  - Verification: 8 new tests (researchSummary.test.js). Smoke-run on prod: 30 papers summarized — 24 emerging / 5 active / 1 too_early, 29 buildable. Spot-checked output quality — summaries are grounded + specific (e.g. RealICU ICU benchmark, Many-Shot CoT-ICL).
- [x] Phase 2.2 — Cross-channel matching v1. New `oied/crossChannelMatch.service.js` links a research paper to opportunities in other channels via keyword/tag overlap (NOT a graph, NOT embeddings — that's Phase 3). extractTerms pulls significant words from title + tags + domains (domain stopword list), scores candidate opps by shared-term count, persists top 8 (overlap ≥ 2) on `aiAnalysis.cross_channel_matches`. Candidate query uses Postgres tag-overlap + title iRegexp, capped 400 rows/opp. Daily cron 7:00 AM UTC, force=true (matches go stale as new opps land). Frontend shows a 🔗 N cross-channel links badge. `AnalysisRun` enum gains `cross_channel_match`.
  - Date: 2026-05-09
  - Verification: 7 new tests (crossChannelMatch.test.js). Smoke-run on prod: 60 research opps → 264 matches linked (142 news, 61 freelance, 39 talent, 9 bonfire, 9 government, 2 capital, 2 strategic).
- [x] Phase 2.3 — research_authors + research_topics tables + extraction. Migration `20260510000007` creates both tables (unique on name/topic_name for upsert). `ResearchAuthor` + `ResearchTopic` models. New `oied/researchAggregation.service.js` does an idempotent full-rebuild FROM the research opps: aggregateAuthors (paper count + summed citation traction + topics + sources per distinct name), aggregateTopics (paper count + recent-vs-prior 30d windows → momentum_score + growth_rate, commercial_score from the high-priority keyword list). getTopAuthors / getTopTopics read helpers. Daily cron 7:15 AM UTC. `AnalysisRun` enum gains `research_aggregation`.
  - Date: 2026-05-09
  - Verification: 7 new tests (researchAggregation.test.js). 123 suites / 1485 tests pass total. Smoke-run on prod: 1126 distinct authors + 25 topics upserted. Top topics by momentum: cs.CL (92), cs.MA (80), cs.AI (80), cs.LG (56).
  - Notes: Phase 2.1 fixup committed separately — `AnalysisRun.type` isIn validation didn't include the new run types (each batch threw "Validation isIn on type failed"); + a Phase 1 fixup for the trends chart data (`dashboard.service` hardcoded `types` arrays) and the MyOpportunities channel dropdown (hardcoded `CHANNEL_OPTIONS`). Phase 2.4 (per-source ingestInterval) + 2.5 (tier-2 adapters) still pending; Phase 3 (pgvector + graph) needs explicit infra sign-off before starting.

## Research Intelligence Expansion — Phase 1 (Research as a first-class channel)

- [x] Adds a new "Research" intelligence channel: AI papers / benchmarks / breakthroughs ingested from arXiv, Semantic Scholar, and HuggingFace Papers flow into the unified opportunities table as `type='research'` and surface everywhere other channels do — channel cards, trends chart, search, classification. Grounded in a codebase recon first; corrected the source plan doc's wrong assumptions (no per-source schedulers exist — research runs on the daily cron; no pgvector — deferred to a later phase; scoring maps onto the existing `aiAnalysis`/`actionType` pattern, not a parallel system).
  - Date: 2026-05-09
  - What changed: `Opportunity.type` model validation + `OPPORTUNITY_TYPES` constant gain `'research'` (no migration — `type` is plain varchar, no CHECK constraint confirmed on prod). `channels.service.js` gains the `research` channel def (indigo, sources arxiv/semantic_scholar/huggingface_papers). 3 new keyless adapters under `ingestion/adapters/research/`: `arxiv.adapter.js` (Atom feed, cs.AI/LG/CL/MA categories, polite 3.2s spacing), `semanticScholar.adapter.js` (Graph API, citation counts → `value`, optional API key via `SEMANTIC_SCHOLAR_API_KEY`), `huggingFacePapers.adapter.js` (daily_papers API, upvotes → `value`). All 3 registered in the `ingestion.service.js` adapter switch. New migration `20260510000006-seed-research-data-sources.js` seeds the 3 `data_sources` rows (enabled, daily cron) — they auto-appear in Source Health. `classification.rules.js` gains `classifyResearch()`: on-topic (matches the expansion plan's high-priority AI keyword list) + has GitHub repo or ≥40 traction → BUILD; on-topic but early → PARTNER; off-topic → IGNORE (still surfaced in the channel, just not actionable). Frontend: `OpportunityChart` TYPE_LINES gains the research line (indigo #6366F1), `Sidebar` gains the Research channel link, `OpportunityCard` gains `research` type color/label + a `ResearchMeta` strip (institution, authors, citations, upvotes, venue, GitHub/PDF links) rendered only for research rows.
  - Verification: 120 backend suites / 1463 tests pass. New `tests/ingestion/researchAdapters.test.js` (9 tests: transform contract for all 3 adapters incl. missing-field defensiveness + no-stable-id skip; 4 classification-rule cases BUILD/PARTNER/IGNORE). `tests/oied/channels.test.js` extended (research → research channel, prod-source coverage, listChannels now 8). 3 modified frontend files babel-parse clean.
  - Notes: Per-source scheduling (arXiv every 2h etc. from the doc) is deliberately deferred to Phase 2 — Phase 1 runs all 3 on the existing daily cron. pgvector/embeddings/semantic-search and the research graph are Phase 3 and need explicit sign-off (new Postgres extension). Adapters are keyless; Semantic Scholar works better with a free API key but degrades gracefully without one. `fetch()` hits live APIs so it's not unit-tested — `transform()` (the contract into the opportunities table) is.

## v0.11 — Hide expired Bonfire opps by default; loud EXPIRED banner for pursued ones

- [x] User feedback: "There should not be a situation where I can see a contract that is expired unless I have already started working on it — in that case, it should be a clear countdown to when it's expiring and a very noticeable expired sign label for it so I know not to waste my time."
  - Date: 2026-05-09
  - What changed: `bonfire.service.listOpportunities` adds a default WHERE clause: `closeDate IS NULL OR closeDate >= now() OR pursuit_status IN ('pursuing', 'submitted')`. Pass `?includeExpired=true` to see the full historical list. `BonfireFilters` exposes a "Show expired" checkbox for admins who want it. `BonfireTable` close-date column replaced with `<CloseCell>` that renders a colored countdown badge: red+bold "EXPIRED · Nd ago", red "CLOSES TODAY", red "Nd left · Mon Day" (≤3d), amber (≤7d), blue (≤30d), grey (>30d). `BonfireSubmissionReadinessPage` adds a prominent red banner above the header card when a pursued opp is past close date (⛔ "This bid has EXPIRED — N days ago"), plus an amber/red countdown banner for opps closing within 7 days.
  - Verification: 22 Bonfire suites / 244 tests pass — 5 new in `tests/bonfire/listExpiredFilter.test.js` covering: default request adds the OR clause; includeExpired=true skips the filter; includeExpired="false" still applies; existing filters (agency/category) are preserved alongside; pursuit_status clause covers ['pursuing', 'submitted']. 3 modified frontend files babel-parse clean.
  - Notes: Pursuit_status='declined' is treated as "no longer pursuing" — declined+expired opps stay hidden by default. Opps with no close_date (rare; some Bonfire postings) always show — we can't know if they're expired. The EXPIRED banner is non-blocking — admin can still navigate the page (Generate Package etc. still work) since they may need to reference historical state.

## Submission Readiness Engine v0.10 — Portal screenshot vision + readiness pivot

- [x] Real-world test against the CARS bid surfaced: (1) the hardcoded 6-doc baseline ("every government bid needs a COI") was inventing requirements that didn't exist for CARS — Part 5 of the RFO explicitly says insurance is "Not applicable for this solicitation"; (2) the actual canonical submission checklist lives on the Bonfire portal page in the "Required Information" table, not in the RFP body; (3) Cloudflare Super Bot Fight Mode on TxDOT's portal blocks every automated scrape we tried (plain headless, stealth plugin, headed Chromium under Xvfb — all stalled on "Just a moment…" indefinitely). v0.10 pivots: rather than fighting CF, embrace the workflow Ali is already doing — open the portal in a real browser, screenshot the Required Information section, drop it in our zone, gpt-4o-mini vision extracts the structured checklist as JSON. That JSON becomes the canonical readiness model; the hardcoded baseline is dropped.
  - Date: 2026-05-09
  - What changed: New `analysis/ai.client.chatVision()` method — sends a base64-encoded image plus text prompt to gpt-4o-mini's chat completions endpoint with json_object response format; same logging/auth pattern as `chat()`. New `bonfire/portalScreenshotExtractor.service.js`: stores the screenshot to disk under `${DOCUMENT_STORAGE_ROOT}/../portal-screenshots/<oppId>/`, calls vision with a tight system prompt that explicitly excludes the "Supporting Documentation" section and only extracts "Required Information" rows (name, file_type, required, conditions, count), sanitizes + caps at 50 rows, persists the result on `submissionRequirements.required_information = { found, rows[], captured_at, screenshot_path, tokens_used }`. New endpoint `POST /opportunities/:id/portal-screenshot` (admin, multer, single PNG/JPG/WEBP up to 20 MB). `bonfireReadiness.computeReadinessState` now graduates to `tailored` when `required_information` is present (regardless of attachments — the agency's published checklist is the truth, attachments just satisfy individual rows). New helper `buildPortalRowItem` matches each portal row against uploaded attachments via lowercased fuzzy substring + significant-word match. Hardcoded `ALWAYS_REQUIRED` baseline is no longer the primary path — it remains as the fallback when neither portal_screenshot nor AI tailoring against attachments has run. Frontend: new `BonfirePortalScreenshotZone.jsx` drag/drop/paste-from-clipboard zone (purple to distinguish from the blue file-upload zone), embedded into `PrePursuitCard` playbook + `PursuingNoAttachmentsCard` (alongside the file zone) + `AttachmentsOnlyCard` (as a tip). FlowSteps updated to 5 steps: Pursue → 📸 Capture requirements → Upload supporting files → Tailor with AI → Generate Package. Stealth plugin (`playwright-extra` + `puppeteer-extra-plugin-stealth`) and Xvfb (in Dockerfile + entrypoint) stay deployed — they don't beat TxDOT's CF but should help on lighter-CF agencies, and the cost is ~10MB.
  - Verification: 21 Bonfire suites / 239 tests pass. Manually confirmed CF block on TxDOT detail page across plain headless / stealth / headed-under-Xvfb / 25-second waits — every variant stalls on "Just a moment…". Backend health checks pass after deploy. Frontend files babel-parse clean.
  - Notes: Vision extraction uses gpt-4o-mini with temperature 0; cost ~$0.001 per screenshot. Screenshot persists to disk so admin can re-extract after a prompt change without asking the user to re-upload. Existing AI tailoring against RFP body (v0.2) is now a complementary signal (finds bid-specific clauses inside the SOW/RFP that aren't in the Required Information table — bonds, EEO, prevailing wage); not the primary checklist source. The fallback to AI-only tailoring (no portal screenshot) is preserved for use on older bids or agencies whose portal page can't be screenshotted.

## Submission Readiness Engine v0.9 (Phase A) — Attachment Classifier + ZIP auto-extract

- [x] Real-world test against the CARS bid ZIP from Ali's downloads exposed two gaps: (1) the user dropped the whole `601440000053067 - pub - Claims Automated Recovery System.zip` and the engine stored it as a single opaque attachment with no extracted text — every inner file (RFO, SOW, Execution of Offer, Family Code Schedule, 3 XLSX schedules) was invisible to AI tailoring; (2) even if individual files had been uploaded, the engine treated them all the same, with no notion that a Pricing Schedule.xlsx is a vendor-fillable schedule vs. an RFO.pdf is a read-only reference. Phase A introduces both: ZIP auto-extract on the way in, plus a per-attachment classifier (read_only_reference / vendor_form / vendor_schedule / other) with field extraction for vendor_form rows.
  - Date: 2026-05-09
  - What changed: `bonfireManualUpload.expandZips()` runs before file processing — uses the existing `jszip` dep (already on the tree via mammoth) to flatten any uploaded ZIPs into one synthetic attachment per inner file, with `metadata.extracted_from_zip = '<zip-name>'` provenance preserved. Skips `__MACOSX` cruft. New `bonfire/attachmentClassifier.service.js` with a fast-path filename heuristic (RFO / RFP / SOW / Q&A / addendum → read_only_reference; XLSX → vendor_schedule; common DOCX/PDF form titles → vendor_form) that short-circuits the AI call for unambiguous cases. AI fallback (gpt-4o-mini, json_object response) for everything else AND for vendor_form rows where we want fillable-field extraction. Persists `classification`, `classification_confidence`, `classification_reason`, `classification_via` (fast_path | ai), `fields[]`, `classified_at` on `metadata`. Auto-runs on every upload via `manualUpload.ingestFiles`. New `POST /opportunities/:id/reclassify-attachments` endpoint to backfill / re-run after a prompt change. `listAttachments` response now exposes the classification fields. Frontend `BonfireAttachmentsPanel` shows colored chips per row (📖 reference · ✍ form · 📊 schedule · — other) with hover-tooltip explaining what the classification means + a 🏷 Reclassify all button at the bottom.
  - Verification: 21 Bonfire suites / 239 tests pass — 13 new in `tests/bonfire/attachmentClassifier.test.js` covering (a) fast-path heuristic for all 4 categories incl. ambiguous fallthrough, (b) sanitizeFields drops invalid + caps at 30 + handles non-array, (c) classifyOne persists fast-path metadata + skips AI when unambiguous, (d) classifyOne still calls AI for vendor_form fast-path rows to get fields, (e) AI fallback for ambiguous filenames, (f) malformed JSON / AI errors → "other" with classification_error logged, (g) classifyAllForOpp orchestrates batch + groups counts. Frontend file babel-parses clean.
  - Notes: ZIP expansion preserves the original ZIP's name in metadata so the manifest can later say "extracted from CARS-RFP.zip." Field extraction uses gpt-4o-mini at temperature 0 with json_object response_format — same shape as v0.2 AI tailoring. Phase B (form filler) consumes the `metadata.fields[]` list per vendor_form to know what to populate from `OrganizationProfile`. Existing legacy attachments (uploaded pre-v0.9) show `classification: null` until an admin clicks 🏷 Reclassify all. Pricing / exceptions schedules are NOT auto-stubbed — that's deferred to Phase C with a conservative scope (only Assumptions, never Pricing or Exceptions).

## Submission Readiness Engine v0.8.1 — Flow polish (silent-no-op fix + Package gate + step indicator)

- [x] Three follow-ups to v0.8 from Ali's feedback: (1) "Tailor with AI doesn't really do anything but leaves a mess" — root cause: when the user pursued + uploaded files, the cache from a prior metadata-only AI run short-circuited a re-run, so `attachment_count` stayed 0 and the panel never graduated to tailored; (2) "Generate Package produces error" — server returned 409 NOT_TAILORED but the button stayed visible; (3) "what is the flow of the order of how things should work" — UI didn't make the four-step sequence visible at all times.
  - Date: 2026-05-09
  - What changed: `bonfireAIRequirements.tailorRequirements` auto-invalidates the cache when prior `attachment_count === 0` but new attachments now exist (`staleByAttachments` flag). The AttachmentsOnlyCard CTA also now passes `force: true` so an explicit click never returns the cached empty result. The Generate Package button is now hidden in non-tailored states on both the dedicated page (replaced with a grey "📦 Package — locked until tailored" pill with tooltip) and the inline drawer (whole submission-package CTA card hidden until state==='tailored'). Added `<FlowSteps>` indicator embedded at the top of every state card: <em>1. Pursue · 2. Upload RFP · 3. Tailor with AI · 4. Generate Package</em> — completed steps go green with a check, the next step glows blue with a ring, future steps are grey. `BonfireReadinessPanel` now exposes `onStateChange` callback; `BonfireSubmissionReadinessPage` and `BonfireDetailPanel` lift the state via the callback to gate the Package button.
  - Verification: 20 Bonfire suites / 226 tests pass (added 1 new test in `bonfireAIRequirements.test.js` that reproduces the silent-no-op bug — runs `tailorRequirements` against a stale `attachment_count: 0` cache with attachments present and asserts a fresh AI call). All 3 modified frontend files parse cleanly via babel.
  - Notes: Step indicator legend: ✓ done · current step has blue ring · future grey. `submitted` state shows all steps green retrospectively. The cache invalidation rule is conservative — only fires when prior run had 0 attachments AND new ones exist, so existing tailored bids aren't re-run unnecessarily.

## Submission Readiness Engine v0.8 — Pursuit-first flow ("Show interest before computing readiness")

- [x] User feedback: "downloading the documents is going to have to be a person… that needs to be the 1st step before you can assess what % it is at." Fundamental flow inversion: until the user clicks Pursue + uploads the actual RFP files, no % is computed (a generic 67% baseline against 6 standard docs is misleading without bid-specific signal). Adds a state machine on top of `bonfire_opportunities` (none → pursuing → submitted/declined), a manual upload zone (Cloudflare bypass: human downloads from the portal, drops files in our drop-zone), and a 4-state readiness panel that points the user to exactly the next action: Pursue → Upload → Tailor → Generate Package.
  - Date: 2026-05-09
  - What changed: New migration `20260510000005-bonfire-pursuit-state.js` adds `pursuit_status`, `pursued_at`, `pursued_by` columns + index. New `bonfire/bonfirePursuit.service.js` (state machine: VALID_STATUSES, ALLOWED_TRANSITIONS, idempotent transition with audit-trail preservation on decline/submit but full reset on `to: 'none'`). New `bonfire/bonfireManualUpload.service.js` ingests multipart files, dedupes by (opp_id, name) for idempotent re-upload, extracts text via pdf-parse / mammoth, persists to `opportunity_attachments` with source='manual', stamps `submission_requirements.last_attachment_fetch = { status: 'manual_upload', via: 'manual' }`. Routes: `GET /pursuit`, `POST /pursue` (admin), `POST /cancel-pursuit` (admin), `POST /attachments` (multipart, admin, multer, 50MB×30 cap, accept PDF/DOCX/XLSX/TXT/CSV/ZIP/images). `bonfireReadiness.service.computeReadiness` now derives a `state` (`pre-pursuit | pursuing-no-attachments | attachments-only | tailored | submitted | declined`) and short-circuits to a state envelope (no checklist, `completion_pct: null`) for everything except `tailored`. `computeReadinessSummaries` does the same for list rows — `pursuit_status` always present, `completion_pct` null until tailored. `submissionPackage.streamPackage` blocks with HTTP 409 + code `NOT_TAILORED` when the bid isn't in the tailored state (admin override via `?override=1`). Frontend: new `BonfireUploadZone` drag/click drop-zone with multi-file staging, 50MB cap validation, inline progress bar, accept-list. `BonfireReadinessPanel` rewritten as a state-machine: `PrePursuitCard` (Pursue CTA + Open RFP link), `PursuingNoAttachmentsCard` (drop-zone embedded, with the Cloudflare-blocked context if applicable), `AttachmentsOnlyCard` (Run AI Tailor CTA + collapsible "add more files"), `SubmittedCard` (historical record), and the legacy tailored UI for state=`tailored`. `BonfireTable.ReadinessCell` now shows `📌 pursuing` / `✅ submitted` / `passed` pills before any %.
  - Verification: 117 backend suites / 1431 tests pass (added `bonfirePursuit.test.js` 9 tests covering all transitions + idempotency + illegal-transition rejection; `bonfireManualUpload.test.js` 6 tests covering create/update/oversize/no_buffer/preserve-prior-status; `bonfireReadiness.test.js` extended with 5 v0.8 state-gate tests + 1 summary-state test; legacy tests adapted to start in `tailored` fixture state). All 3 modified frontend files parse cleanly via babel.
  - Notes: Existing opps default to `pursuit_status='none'` after migration — every Bonfire opp resets to "pre-pursuit" view. Acceptable since the prior baseline % was misleading anyway. Admin override on Generate Package (`?override=1`) is a safety belt for emergency packaging when AI tailoring is broken; not exposed in the UI yet (deliberate friction). The drop-zone enforces 50 MB × 30 files; oversize files are rejected client-side (size check) AND server-side (multer limit + service guard) — defense in depth. Manual-upload flow is the explicit Cloudflare bypass: when blocked, the panel walks the user through opening Bonfire → downloading → dropping. No new dependencies.

## AI Tools page — fix stuck-click bug ("takes 3–4 clicks to navigate")

- [x] User report: clicking a card on `/ai-tools` "doesn't always pop up what I'm clicking on … takes 3-4 clicks." Root cause: the page rendered the tools grid as `{!loading && tools && tools.length > 0 && ...}` and skeletons as `{loading && ...}`, so any refetch (`fetchAiTools.pending` flips `loading: true`) unmounted the entire grid mid-interaction. If a refetch fired between mousedown and click, the `<Link>` you clicked was no longer in the DOM and the navigation never landed. Fix: keep cards mounted across refetches with reduced opacity, render skeletons only on cold-start (no data yet).
  - Date: 2026-05-09
  - What changed: `frontend/src/pages/AiToolsPage.jsx` — skeleton block now gated on `loading && (!tools || tools.length === 0)` so it only shows on initial load; tools grid is always mounted whenever data exists, with `opacity-60` + `transition-opacity` while loading. Also tightened the React `key` to fall back to `tool.slug` then index if `tool.id` is missing, so reconciliation never silently re-uses DOM nodes for different tools.
  - Verification: `tsc --noEmit` clean (frontend), prod redeploy `op-frontend` smoke-tested at <a href="http://95.216.199.47/ai-tools">/ai-tools</a> — cards remain clickable through filter changes; no DOM unmount on refetch.
  - Notes: This is a pure UI-stability fix — no API change, no slice change. Same pattern would help any other list page that swaps data-grid ↔ skeletons on `loading`. If the issue persists after this fix, next step would be to convert the card to programmatic `useNavigate()` onClick in addition to the Link's default href, but holding off until we see whether this resolves it.

- [x] Two complementary additions: (a) the daily Source Health Agent email now includes vault doc expiry alerts (🚨 Expired + ⏳ Expiring within 30 days sections), so renewals are surfaced before they bite a submission; (b) a dedicated `/admin/bonfire/:id/submission-readiness` page that gives one bid a full-screen view with linkable URL — handy for sharing a single bid's status with a teammate without screenshotting the drawer.
  - Date: 2026-05-08
  - What changed: `admin/sourceHealthAgent.service`: new `loadExpiringDocs()` helper pulls docs expiring within 30 days via `documentService.listDocuments({expiringWithinDays: 30})`, splits into `expired` (days < 0) and `expiring_soon` (days 0–30), sorts most-urgent first. Report now carries `expired_docs` + `expiring_docs` arrays. `should_email` flips true when either is non-empty. HTML email body adds 🚨 Expired (red) + ⏳ Expiring (amber) sections per doc with type label + name + version + scope + days math; text body mirrors. New page `frontend/src/pages/BonfireSubmissionReadinessPage.jsx` mounted at `/admin/bonfire/:id/submission-readiness`: header card with title/agency/close-date + Generate Package button + priority/value badges + linked Bonfire RFP, two-column layout (readiness + attachments left, quick-links sidebar right), reuses the existing `BonfireReadinessPanel` and `BonfireAttachmentsPanel` components verbatim. Deep-link button "📑 Open full readiness page" added to the Bonfire detail drawer header.
  - Verification: 4 new tests in `tests/admin/sourceHealthAgent.test.js` (expired-only triggers email, expiring-only triggers email, clean state stays should_email=false, email body includes both sections with correct content). Full backend suite: 115 suites / 1409 tests pass. Frontend syntax-checked.
  - Notes: Expiry scan runs scoped to `organizationId: 1` for now (single-org prod). Multi-org rollout would group by org_id and emit one email per recipient. The dedicated readiness page is purely additive — the inline drawer panel still works exactly the same; admins choose by context. Sidebar quick-links + Tip help with first-time orientation.

- [x] v0.7 walkthrough HTML for Ali's review.
  - Date: 2026-05-08
  - What changed: New `docs/submission-readiness-v07-walkthrough.html` mirroring the established walkthrough pattern (sticky TOC, embedded CSS, click-to-zoom screenshots, per-section feedback textareas with localStorage + Copy All to clipboard). Sections: Cover, What changed, Dedicated readiness page (live screenshot), Two-column layout, How to reach the page, Vault expiry alerts, Email HTML mockup, Email plain-text version, Email gating logic, Files changed, Final notes (engine state recap + remaining SAM.gov/grants.gov fetcher work). New capture script `backend/scripts/capture-submission-readiness-v07-walkthrough.js` (Playwright) + screenshot at `docs/submission-readiness-v07-walkthrough-assets/01-dedicated-readiness-page.png` showing the Records Management System bid at 50% complete on the dedicated page.
  - Verification: HTML file opens in browser; TOC links resolve; screenshot loads; feedback textareas + Copy All wired correctly (matches v0.4/v0.5/v0.6 walkthrough JS).

## Submission Readiness Engine v0.7.1 — UI honesty (Cloudflare-blocked status + baseline vs AI-tailored framing)

- [x] Addresses Ali's feedback that "Fetch from Bonfire button doesn't work" and the deeper question of whether the system actually knows what's required before reading the RFP PDFs. Diagnosed the button: it does work — but Cloudflare blocks ~80% of Bonfire detail pages (HCCS, Round Rock, Richardson, NATO all confirmed blocked on prod), and the UI was silent about it. Now: (a) blocked status persists across reload via `submission_requirements.last_attachment_fetch`; (b) panel shows an honest amber callout when blocked; (c) readiness panel carries a Baseline / AI-tailored / AI-tailored-with-attachments pill so users know which level of accuracy they're looking at.
  - Date: 2026-05-08
  - What changed: `bonfire/attachmentFetcher.service` adds `stampLastFetch(opp, {status, result})` helper called from every code path (blocked, no-links, success, failed) — persists `submissionRequirements.last_attachment_fetch = { status, at, found, saved, blocked }` on the opp row alongside `attachmentsFetchedAt`. `bonfire/bonfire.controller.listAttachments` response gains `last_attachment_fetch` + `attachments_fetched_at` fields. `bonfire/bonfireReadiness.service` exposes `ai.attachments_used` + `ai.attachment_count` so the UI can distinguish "AI inferred from title+description" from "AI read the actual RFP." `frontend/components/bonfire/BonfireAttachmentsPanel.jsx`: persistent amber callout when blocked (`⚠ Cloudflare blocked this portal …`), neutral grey note when no documents listed, success banner when files actually arrived; pre-fetch hint adds "(Cloudflare blocks ~80% of agency portals — don't be surprised if it returns 0.)" so the user knows what to expect. `frontend/components/bonfire/BonfireReadinessPanel.jsx`: explicit pill below the % bar — "📋 Baseline" or "🤖 AI-tailored (inferred from title + description)" or "🤖 AI-tailored (read from fetched documents)." Walkthrough HTML extended with two new sections (Cloudflare-blocked status + Baseline vs AI-tailored) and an "Open in app" section with deep-links to every surface mentioned.
  - Verification: 18 Bonfire suites / 203 tests pass (no breaking changes; existing readiness tests still pass with the additive `attachments_used` field). Frontend syntax-checked. Cloudflare-block reproduced on prod against 4 different agencies via direct `fetchOneBonfireOpp()` invocation in the op-backend container — all returned `blocked: true, attachments_found: 0`.
  - Notes: This is a UI-honesty patch, not a Cloudflare bypass — bypass would need either (a) a paid CF-friendly proxy, (b) per-agency manual cookie capture, or (c) a residential-proxy rotation. None of those are scoped now. Bonfire stays a "best-effort" platform; SAM.gov + grants.gov don't have the same problem and will work cleanly when their fetchers ship.

## Submission Readiness Engine v0.6 — Auto-fill from evergreen vault (Phase 6)

- [x] When the actionGenerator runs (proposal / offer / resume), it now reads relevant vault docs (capability statement, past performance, references) and injects excerpts into the AI prompt under an "Approved Source Material" block. Generated proposals cite real org artifacts instead of inferring everything from the JSON profile fields. Lazy text extraction layer means PDF + DOCX vault docs become readable too — first read extracts via pdf-parse / mammoth and caches on `documents.metadata.extracted_text` for next time.
  - Date: 2026-05-08
  - What changed: New `documents.document.service.getOrExtractText(doc)` — reads file via storage adapter, extracts based on mime (pdf-parse for PDF, mammoth for DOCX, utf8 for markdown/text), caches on `metadata.extracted_text` + `metadata.extracted_at` for next call. New `documents.document.service.loadVaultExcerpts({ organizationId, types, bonfireOpportunityId, capPerDoc, capTotal })` — pulls active vault docs of the requested types (local-for-bid takes precedence over global on dedupe), returns capped excerpts ready for prompt injection. `oied/actionGenerator.service` now: defines per-type vault type lists (`VAULT_TYPES_FOR_PROPOSAL = [capability_statement, past_performance, references]`, offer + resume narrower); loads excerpts before each AI call (skipped for analysis type); appends them via a new `appendVaultBlock()` helper into both `buildUserPrompt` and `buildGroundedUserPrompt`; persists an audit trail on `OpportunityOutput.metadata.evergreen_docs_used` (id + type + scope + chars per injected doc).
  - Verification: 5 new tests in `tests/documents/loadVaultExcerpts.test.js` (no-types short-circuit, dedupe by type with bid-priority, cache hit avoids fs read, missing-file skip, capTotal stop) + 4 new tests in `tests/oied/actionGenerator.test.js` (proposal prompt contains Approved Source Material + injected text, metadata.evergreen_docs_used populated with id/type/scope/chars, analysis skips vault, empty vault leaves metadata.evergreen_docs_used = []). Full backend suite: 115 suites / 1405 tests pass.
  - Notes: Cap-per-doc default 2 KB, cap-total 10 KB — enough for a capability statement excerpt + a past-performance summary + a references list without blowing the context window. Extraction is lazy + cached so cold reads on PDF vault docs pay the parse cost once. AI client unchanged (gpt-4o-mini, temp 0.4); the upgrade is purely on the prompt-building side. Pure additive — empty vault leaves prompt identical to v0.5.

## Submission Readiness Engine v0.5 — Submission Package Assembler (Phase 4)

- [x] One-click "📦 Generate Package" button on the Bonfire detail panel that streams a single ZIP for upload to the agency portal: every vault doc (global + bid-local), every AI-generated draft additionally rendered to PDF for procurement-acceptable upload, every fetched RFP attachment, plus a human-readable README cover page with the readiness checklist and a JSON manifest. End-to-end the readiness panel goes from "track docs" to "deliver the bid."
  - Date: 2026-05-08
  - What changed: Three new pure-JS deps (archiver 8.x, pdfkit 0.18.x, markdown-it 14.x — no Chromium, no native binaries). New `documents/documentRenderer.service.js`: walks markdown-it tokens and emits pdfkit drawing commands (headings, paragraphs, bold/italic/inline-code, ordered+bulleted lists, hr, fenced code blocks). Outputs a Letter-size PDF buffer. New `bonfire/submissionPackage.service.js`: assembles the ZIP — README.md + manifest.json + /vault (global subdir + bid-local subdir, original file + auto-rendered PDF for any markdown source) + /attachments (originals from v0.4). New endpoint `GET /api/v1/bonfire/opportunities/:id/submission-package` (admin) streams via archiver. Frontend: `bonfireAttachmentsService.downloadSubmissionPackage()` helper, new "📦 Submission Package" admin-only banner on `BonfireDetailPanel` with `📦 Generate Package` button that triggers the download. Filename: `submission-package_<title-slug>_<date>.zip`. archiver is lazy-required inside the streamPackage function so it doesn't poison test environments that don't exercise the ZIP path.
  - Verification: 9 new tests across `tests/documents/documentRenderer.test.js` (3: PDF magic header on simple md / lists+hr+code / empty input) and `tests/bonfire/submissionPackage.test.js` (6: README content + AI-flagged checklist marking, manifest schema_version + readiness summary + vault entries + attachment entries, safeName strips disallowed chars + caps length + handles null). Full backend suite: 114 suites / 1396 tests pass.
  - Notes: PDF rendering is intentionally lightweight — pdfkit + markdown-it walker, no Chromium. Trade-off: complex markdown (tables, deep-nested lists) renders best-effort. Adequate for proposals / cover letters / capability statements; if we hit cases that need richer rendering, swap in a puppeteer-based pipeline later. README + manifest formats are versioned (schema_version: 1) so downstream consumers can lock onto them.

## Submission Readiness Engine v0.4 — Bonfire RFP attachment locker

- [x] Per-opp Bonfire detail-page scraping that downloads the actual RFP attachments (SOW, Q&A, amendments, schedules) into a per-opportunity locker, extracts text via pdf-parse / mammoth, and feeds that text into the v0.2 AI requirement detector. Closes the data-starved loop: AI tailoring now sees the actual RFP body and starts surfacing real bond / EEO / prevailing-wage requirements with quoted evidence instead of returning "no additional requirements" on every bid.
  - Date: 2026-05-08
  - What changed: New `opportunity_attachments` table (migration `20260510000004-create-opportunity-attachments.js`) — UUID PK, polymorphic-ish `bonfire_opportunity_id` + future `opportunity_id`, source enum, `parsed_text` cache, partial-unique index on (bonfire_opportunity_id, url_original) for idempotency. Extended `bonfire_opportunities` with `attachments_fetched_at` so we don't keep retrying empty pages. New Sequelize `OpportunityAttachment.js` model. New `bonfire/scraper/pages/opportunityDetail.js` parser (cheerio-based dual-mode: `parse(page)` for Playwright + `parseHtml(html)` for tests; matches `/file/`, `/document/`, `.pdf`, `.docx`, `.xlsx` anchors with dedup). New `bonfire/attachmentFetcher.service.js` orchestrator: launches the existing scraper browser context, ensures login via existing `session.js`, navigates to opp.sourceUrl, parses links, downloads each via Playwright's authenticated `context.request` (so session cookies travel), saves to `./uploads/attachments/opportunities/<oppId>/`, extracts text per mime (pdf-parse / mammoth / utf8), upserts rows idempotently. `bonfireAIRequirements.service.buildUserPrompt` now appends parsed attachment excerpts (capped 4 KB/file, 20 KB total) to the AI prompt — the unlock that lets v0.2 actually flag real requirements. Three new endpoints: `POST /api/v1/bonfire/opportunities/:id/fetch-attachments` (admin), `GET /opportunities/:id/attachments`, `GET /opportunities/:id/attachments/:attachmentId/download`. Frontend: new `BonfireAttachmentsPanel.jsx` injected into `BonfireDetailPanel` between the readiness panel and stats — lists files with download links, "parsed" badge for files with extracted text, "⏬ Fetch from Bonfire" admin button, last-fetch result banner.
  - Verification: 13 new tests across `tests/bonfire/opportunityDetailParser.test.js` (6: link extraction, dedupe, filename fallback, empty/null safe, multi-format anchors) and `tests/bonfire/attachmentFetcher.test.js` (7: mime inference cases + upsert idempotency create vs update). Full backend suite: 112 suites / 1387 tests pass.
  - Notes: Scope is **Bonfire-only on-demand** for v0.4. SAM.gov + grants.gov detail-page fetchers come in v0.5 (separate adapter shapes). No automatic bulk scrape — admin clicks Fetch per opp. Re-fetching the same URL replaces the existing row (partial unique index). Files persist on the existing `op-uploads` named volume under `attachments/`. Largest file capped at 50 MB. AI excerpts capped at 4 KB/file × 20 KB total to fit the prompt window.

## Submission Readiness Engine v0.3 — AI-generated docs + local/global scope

- [x] Two paired changes from round-3 feedback: (a) 🤖 Generate button on the Bonfire readiness panel for any document type that's AI-generatable (cover letters, capability statements, EEO statements, NAICS lists, references, past performance summaries, technical/pricing response templates, non-collusion affidavits, prevailing-wage certs, addendum/site-visit acks). The button is HIDDEN for must-be-obtained types (W-9, COI, agency certs, surety bonds, professional licensure, cybersecurity assessments, etc.) so users never click a button that can't deliver. (b) Documents now have a scope: 'global' (vault, current behavior) or 'bid' (local to one Bonfire opportunity). When AI generates a doc for a specific bid, the system writes TWO linked rows: a local-to-bid row (counts toward THAT bid's checklist) and a global version-history row (so the org accumulates a corpus of generated content for analysis + improvement over time). Both rows share a `lineage_id`.
  - Date: 2026-05-08
  - What changed: Migration `20260510000003-documents-scope-and-lineage.js` adds 4 columns to `documents` (scope STRING, scope_id STRING, lineage_id UUID, source STRING). `documentTypes.js` adds a `generatable` boolean per type + `isGeneratable()`/`getGeneratableKeys()` helpers (13 types generatable; the rest correctly flagged as must-be-obtained). New `documents/documentGenerator.service.js` with per-type system prompts (paragraph each) for all 13 generatable types, plus a single `generateDocument({type, organizationId, bonfireOpportunityId})` entry point that loads opp + profile, calls the AI, and writes both rows. `document.service.activeTypeMap` is now scope-aware: when a `bonfireOpportunityId` is passed, local-for-this-bid docs take precedence over globals on dedupe. `document.controller` adds scope params on upload + a new `POST /api/v1/documents/generate` endpoint + `GET /api/v1/documents/types/generatable`. `bonfireReadiness.service` reads with the bid's id so local docs satisfy the checklist; checklist items now carry `type_generatable` + `document.scope` + `document.doc_source` for UI rendering. Frontend: `BonfireReadinessPanel` shows a 🤖 Generate button on every gap row whose type is generatable, plus per-row scope chips (📌 this bid / 🌐 vault) and a 🤖 AI badge for AI-generated docs. `DocumentVaultPage` adds Scope + Source filters and renders the same scope/source pills inline on each row. `documentService.js` (frontend) gets `generateDocument()` + `listGeneratableTypes()`.
  - Verification: 10 new tests in `tests/documents/documentGenerator.test.js` (NOT_GENERATABLE guard, unknown types rejected, single-row global path when no bid, dual-row local+global path with shared lineage when bid is passed, NOT_FOUND for missing opp, AI_FAILED on rate-limit, EMPTY_CONTENT rejection, global version auto-bump, prompt assembly with/without opp context). Full backend suite: 110 suites / 1374 tests pass.
  - Notes: Two physical files written per AI generation (small markdown, disk is cheap; keeps each row independent if one is later deactivated/promoted). Scope='bid' takes precedence over 'global' on the readiness dedupe so the bid's local copy is what the user sees. The bulk readiness-summaries endpoint stays global-only for now (per-bid local lookups would 25× the query count); per-bid detail panel is fully scope-aware. Generated content is plain markdown (no PDF rendering in v0.3 — that's Phase 4 of the master plan).

## Submission Readiness Engine v0.2 — Per-opp AI requirement tailoring (Bonfire)

- [x] Layer AI on top of v0.1's fixed Bonfire checklist: when a Bonfire RFP requires a bid bond, prevailing wage cert, EEO statement, professional licensure, OSHA/safety record, cybersecurity assessment, MWBE/DBE cert, etc., the readiness panel now flags it for THAT bid only and quotes the RFP text that supports each flag. Confidence-thresholded so weak signals get dropped. Cached on the row so the AI runs once per opp; admin can hit "Refresh AI" to regenerate.
  - Date: 2026-05-08
  - What changed: New JSONB column `bonfire_opportunities.submission_requirements` (migration `20260510000002-bonfire-submission-requirements.js`). Document type registry extended with 11 new types: bid bond / payment bond / performance bond / Davis-Bacon prevailing wage / EEO statement / proof of licensure / safety program / cybersecurity assessment / site visit acknowledgment / addendum acknowledgment / non-collusion affidavit. New service `backend/src/bonfire/bonfireAIRequirements.service.js`: builds a structured prompt with the canonical type list as a controlled vocabulary, calls the existing AI client (gpt-4o-mini, temperature 0.1, response_format: json_object), validates output (drops items without source_quote, drops invented types, clamps confidence to [0,1]), persists on the row. New endpoint `POST /api/v1/bonfire/opportunities/:id/tailor-requirements` (admin-only, body `{ force: true }` to regenerate). `bonfireReadiness.service` now merges baseline (6 types) + regex-conditional (MWBE) + AI-detected (filtered at confidence ≥ 0.5) deduped on type; AI takes precedence on display. Each checklist item carries `source` ('baseline' | 'conditional' | 'ai'), `source_quote`, and `confidence`. Frontend: `BonfireReadinessPanel` adds a "🤖 Tailor with AI" button (becomes "Refresh AI" once cached), an AI-summary banner, source pills on each checklist row, and an inline RFP-quote block for AI-flagged items.
  - Verification: 17 new tests across `tests/bonfire/bonfireAIRequirements.test.js` (5 validateAndFilter cases, 1 prompt assembly, 6 tailorRequirements cases incl. cache hit/force/JSON-fail/AI-error/NOT_FOUND, 2 confidence-threshold cases) and `tests/bonfire/bonfireReadiness.test.js` (3 v0.2 merge cases incl. regex+AI dedupe). Full backend suite: 109 suites / 1364 tests pass. Backend + frontend syntax-checked. Prod deploy + migration below.
  - Notes: Hard rule in the system prompt forces Claude to quote RFP text for every flag — items without a quote are dropped at the validation layer. Confidence threshold of 0.5 is configurable via the readiness service constant. Type vocabulary is bounded — Claude can only flag types from the canonical list (or "other" with a name). AI calls are cached on the row; first read after regenerate is the only one that costs a token.

## Submission Readiness Engine v0.1 — Document Vault + Bonfire readiness %

- [x] First slice of the Submission Readiness Engine: an evergreen Document Vault (W-9, COI, capability statement, certifications, past performance, etc.) plus a per-Bonfire-bid checklist that computes completion % from what's in the vault. Inline progress bar on every Bonfire row + a full Submission Readiness panel inside the detail drawer with status pills (✓ on file / ⚠ expiring / ✗ expired / ✗ missing) and one-click jump-to-vault for gaps.
  - Date: 2026-05-08
  - What changed: New `documents` table (migration `20260510000001-create-documents.js`) + Sequelize `Document` model. New `backend/src/documents/` module: `storage.adapter.js` (local disk under `${DOCUMENT_STORAGE_ROOT}/<org>/<type>/<uuid>.<ext>`, S3-ready interface), `documentTypes.js` (canonical 18-type registry mapped to platforms), `document.service.js`, `document.controller.js`, `document.routes.js` mounted at `/api/v1/documents` (admin-only). New `backend/src/bonfire/bonfireReadiness.service.js`: fixed 6-doc Bonfire baseline checklist (`cover_letter_template`, `capability_statement`, `coi`, `references`, `technical_response_template`, `pricing_response_template`) plus regex-detected MWBE/DBE conditional. Two new endpoints on bonfire routes: `GET /opportunities/:id/readiness` (per-bid checklist) + `POST /opportunities/readiness-summaries` (bulk for list pages). Frontend: `DocumentVaultPage.jsx` at `/admin/documents` with upload modal + grouped table + expiry summary cards; sidebar entry **📁 Document Vault** under Operate. `BonfireReadinessPanel.jsx` injected into `BonfireDetailPanel` (admins only). `BonfireTable` adds a Readiness column with inline progress bar driven by bulk-summary fetch on page load. `docker-compose.prod.yml` mounts a new `op-uploads` named volume at `/app/uploads` so docs persist across deploys.
  - Verification: 10 new tests in `tests/bonfire/bonfireReadiness.test.js` (MWBE conditional detection, 100% completion path, partial completion, expired/expiring states, MWBE-triggered checklist expansion, NOT_FOUND error, bulk summaries). Full backend suite: 108 suites / 1347 tests pass. Backend + frontend syntax-checked. Prod deploy + migration below.
  - Notes: v0.1 uses a fixed Bonfire checklist; per-opp AI tailoring (e.g. "this RFP requires bond-of-bid → add bonding doc") is v0.2. Re-uses existing multer (now with disk storage), pdf-parse, mammoth (no new deps). No S3 in v0.1 — local disk + named volume; storage adapter abstracts later swap. Document categorization is `STRING(60)` not enum so adding new types is code-only (no migration).

## OIED v9.10 — Source Health Auto-Triage Agent

- [x] Daily cron + on-demand button that retries every failing data source once, classifies what's still broken into actionable categories, and emails the operator a summary — but only when there's something worth reading. The follow-up to v9.9 (Source Health page); now the page can ALSO act on what it surfaces.
  - Date: 2026-05-07
  - What changed: `backend/src/admin/sourceHealthAgent.service.js` runs the triage flow: snapshot health → retry every failing source via `runIngestion()` → re-snapshot → classify residual failures into `missing_credentials` / `schema_overflow` / `upstream_validation` / `upstream_unavailable` / `feed_url_dead` / `unknown` (regex rules over the rendered last_run_error) and `source_dried_up` for zero_yield rows. `runAndEmail` composes a focused HTML+text email and sends via the existing `utils/email.js` (nodemailer); skipped on clean days. `sourceHealthAgent.scheduler.js` adds a daily cron (default `0 7 * * *` UTC, an hour after the master ingestion cron) gated by `OIED_SOURCE_HEALTH_AGENT_ENABLED`. `sourceHealthAgent.controller.js` exposes `POST /api/v1/admin/source-health-agent/run` (admin-only) for on-demand runs from the page. Frontend: new "🤖 Run Auto-Triage" button next to "Refresh" on `DataSourceHealthPage`, plus a `TriageReportCard` that renders the inline result with recovered/still-failing/zero-yield groupings and the suggested human action per row. `docker-compose.prod.yml` passes through `OIED_SOURCE_HEALTH_AGENT_*` and `OIED_PUBLIC_URL` env vars.
  - Verification: 17 new tests in `tests/admin/sourceHealthAgent.test.js` (11 classifier cases, 3 runAgent cases incl. retry-only-on-failing and zero_yield-no-retry, 3 email-gating cases incl. clean-day-skip, no-recipient-skip, happy-path send). Full backend suite: 107 suites / 1337 tests pass. Backend + frontend syntax-checked. Smoke pending on prod.
  - Notes: Conservative auto-fix surface — never edits credentials, schema, feed URLs, or config. The agent's job is to TRIAGE, not to repair. Default OFF until a recipient is configured (`OIED_SOURCE_HEALTH_AGENT_TO`); falls back to `OIED_BRIEFING_TO` if set. No schema changes. Re-uses existing nodemailer transport.

## OIED v9.9 — Data Source Health page

- [x] New ops page surfacing every ingestion data source with derived status (healthy / stale / zero_yield / failing / disabled), last-run details, recent created-row totals, 14-day sparkline, and a "Run now" trigger. Solves the silent-stall blind spot that left the capital channel dead for 80 days before anyone noticed.
  - Date: 2026-05-07
  - What changed: New backend controller `admin/dataSourceHealth.controller.js` with two endpoints (`GET /api/v1/admin/data-sources/health`, `POST /api/v1/admin/data-sources/:name/run`). Status derivation: `disabled` (flag off), `failing` (last run failed OR 2 of last 3 runs had errors[]), `stale` (no successful run in >48h), `zero_yield` (3+ successful runs in a row with recordsCreated=0), `healthy`. Rolls up `rows_24h` / `rows_7d` / `rows_30d` from `ingestion_logs` and pads a `trend_14d` array zero-filled per day. Source → channel mapping derived live from `Opportunity` group-by on (type, source). Next-run time computed from `INGESTION_SCHEDULE` env via a small inline cron-next-fire scanner (handles `*`, lists, ranges, steps — no new dependency). New frontend page `pages/DataSourceHealthPage.jsx` with status pills, 14-day inline-SVG sparklines, expandable error details, "Run now" buttons, summary cards (failing / stale / zero_yield / healthy / disabled), and a 60-second auto-refresh. Sidebar entry **🩺 Source Health** under Operate. Mounted at `/admin/data-sources`, admin-only.
  - Verification: 14 new unit tests in `tests/admin/dataSourceHealth.test.js` (status derivation: 8 cases; nextCronFire: 6 cases). Full backend suite 106 suites / 1320 tests pass. Backend + frontend syntax-checked. Smoke verified after deploy: page renders, identifies the just-fixed funding_news source as healthy with 26 rows in last 24h, sparkline shows the spike on 2026-05-07.
  - Notes: Re-uses existing `IngestionLog` model — no schema migration. Re-uses `runIngestion()` for the manual trigger, gated through admin RBAC. Auto-refresh interval is 60s — light enough to leave the page open during a deploy and watch for green/red flips.

## OIED v9.8.2 — Type-aware action gating + resume generation + capital feed fix

- [x] Three changes from Ali's round-3 walkthrough: (a) hide Generate Proposal / Generate Offer on rows where they make no sense (News, Capital — they're informational/research, not bidable); (b) replace those buttons with "Tailor Resume" on Talent rows so the Pursue action is to apply for the job, not bid on it; (c) capital channel had been silently stalled since 2026-02-18 because the TechCrunch `/category/fundraise/feed/` URL started returning 404 — swapped feed list and updated the prod DataSource row.
  - Date: 2026-05-07
  - What changed: `OpportunityOutput.type` validate enum extended with `'resume'`; `actionGenerator.ALLOWED_TYPES` + `SYSTEM_PROMPTS` now include resume (prompt: "## Summary | ## Skills | ## Experience | ## Education / Certifications" tailored to job description, sourced from user profile services/tools/past wins, flagging gaps if profile is sparse). `OpportunityActionButtons` now takes an `oppType` prop and gates: `ai_news` + `investment` → analyze only; `ai_job` → analyze + tailor resume; everything else → legacy proposal+offer+analyze. `MyOpportunitiesPage` passes `oppType={opp.type}`. `fundingNews.adapter.DEFAULT_FEEDS` swapped from broken `techcrunch.com/category/fundraise/feed/` to working `techcrunch.com/category/venture/feed/` + `news.crunchbase.com/feed/` + (kept) `venturebeat.com/category/ai/feed/`. Existing prod `data_sources` row updated in-place during deploy.
  - Verification: 460 OIED tests pass (+1 resume test). Backend + frontend syntax-checked. Smoke probe of the new feeds returned items dated 2026-05-07. After deploy, the funding_news ingest run produced fresh capital rows (vs. zero on prior runs since Feb).
  - Notes: Resume type rides free in billing (no proposals_generated meter). No schema migration — `OpportunityOutput.type` is a STRING(20) with a validate-isIn array, no DB enum to alter. Backwards compatible: existing 'proposal' / 'offer' / 'analysis' calls untouched.

## OIED v9.8.1 — Industry visibility in cloud + emoji bullets in channel cards

- [x] Round-3 keyword-cloud feedback: industries weren't appearing at the top level (cloud was dominated by talent/freelance fluff like "engineer", "team", "senior") and the channel-bucket cards were a bare wall of titles. Fixed the read path to guarantee an industry quota and added per-row content emoji to the bucket cards.
  - Date: 2026-05-07
  - What changed: `intelligence.controller.getKeywordCloudHandler` now does two queries when `industries_only=false`: top `(max - quota)` general words + top `quota` industries, deduped and merged. Quota = `max(8, max/3)`. `newsWordCloud.service.STOP_WORDS` adds a narrow set of job-listing fluff (across, including, role, senior, team, freelance, etc.) — kept narrow so legitimate hot terms like "engineer" / "developer" stay. New frontend module `components/oied/titleEmoji.js` mapping ~35 keyword regexes → content emoji (🚂 railway, 🏥 hospital/clinical/medical, 🪖 defense/military/veterans, 🏛️ government/agency/department, 💰 finance/capital, ⚡ energy, 🤖 ai/agentic, 🧠 claude/anthropic, ☁️ aws/azure/cloud, 🔒 security, etc.). `ChannelBucketStrip` now renders rows as `<ul>` with each `<li>` led by `emojiForTitle(title)`.
  - Verification: 459 OIED tests pass (no test changes — additions are additive on the read path and the frontend). Backend + frontend syntax-checked. Prod deploy + recompute below.
  - Notes: Industry quota only kicks in when `industries_only` is false (the default). When toggle is on, the response is still industries-only. Re-uses existing `is_industry` flag — no schema change.

## OIED v9.8 — Drill-down sub-cloud + true sentiment + channel-bucket strip

- [x] Three-pronged fix for the keyword cloud: (1) red→green gradient with gray for unknown — replaces the olive-everywhere look from v9.7; (2) per-channel breakdown card-strip on keyword search results so news matches surface in row 1 instead of being buried 18 pages deep; (3) drill-down sub-cloud above search results that recomputes a focused mini-cloud from the matching opps and lets a click append the sub-word to `q` (multi-needle AND filter).
  - Date: 2026-05-07
  - What changed: `keywordCloud.service` now tracks `sentSum` and `sentCount` separately so sentiment is averaged over sentiment-bearing samples only — non-news sources pass `sentiment: null` and don't dilute the score. New `getDrillDownCloud({q, max})` helper aggregates a sub-cloud from opps that match parent keyword(s) (AND semantics for comma-separated tokens). New endpoint `GET /api/v1/oied/keywords/drill?q=<word>(,<word>...)`. `myOpportunities.service` now: (a) accepts comma-separated `q` and ANDs each token at the SQL level, (b) emits `channelBuckets` (per-channel top-5, recency-sorted for news/talent, priority-sorted otherwise) when `q` is set. Controller threads `channelBuckets` through `paginatedResponse.pagination`. Frontend: new `KeywordDrillCloud` component (clickable refine-by-subword), new `ChannelBucketStrip` on `MyOpportunitiesPage`. `KeywordCloud.jsx` color function rewritten: hue 0→120 (no olive midpoint), gray when `sentiment_known === false`. Cloud read path emits `sentiment_known` from the persisted `sentiment_label === 'unknown'` marker (no schema migration — re-uses existing `VARCHAR(20)` column).
  - Verification: 5 new tests in `tests/oied/keywordDrillCloud.test.js` (empty q, multi-token AND, parent exclusion, sentiment_known true on news, sentiment_known false on non-news). Full OIED suite 33 suites / 459 tests pass. Backend syntax-checked via `node -c`; frontend (incl. new JSX component) syntax-checked via `@babel/parser`. Prod deploy + recompute below.
  - Notes: Re-uses the existing `keyword_trends.sentiment_label` column to mark "unknown" — no migration needed. Drill-down endpoint is read-only and bridge-gated. Multi-needle q is opaque to existing single-token callers (they keep working).

## OIED v9.7 — Trending keywords persistence (validated keyword cloud)

- [x] Replace live keyword-cloud aggregation with twice-daily persisted snapshot. Every word now passes a validation gate (`match_count >= 3` opp matches OR `tool_count >= 1` AI-tool matches) before it can appear in the cloud, so click-throughs are guaranteed to have results. Adds curated industry classification (~80 verticals, `is_industry` flag), a per-word display name (title-cased), and an industries-only UI toggle. Sidebar "Dashboard" link is hidden for admins (single-canonical Mission Control). Cross-channel keyword search now does a per-channel sub-pool (~80 rows × 7 channels) so a click on "healthcare" surfaces matches in news, capital, talent, etc. — not just the most-recently-updated bucket.
  - Date: 2026-05-07
  - What changed: New table `keyword_trends` (migration `20260509000001-oied-keyword-trends.js`) + Sequelize model `KeywordTrend.js` + compute service `keywordTrendCompute.service.js` (validates every candidate against `Opportunity.count` + `AiTool.count` ILIKE, drops zero-match words, persists via `bulkCreate updateOnDuplicate`, soft-retires prior-run rows). Twice-daily cron `keywordTrend.scheduler.js` (default `15 5,17 * * *` UTC) gated by `OIED_KEYWORD_TRENDS_ENABLED`. New admin endpoint `POST /api/v1/oied/keywords/recompute` for on-demand refresh. `getKeywordCloudHandler` now reads from `keyword_trends` first (fallback to live aggregator if empty / >36h stale / table missing), supports `industries_only=true`. `KeywordCloud.jsx` adds Industries-only checkbox, prefers `display_word`, shows "validated · updated <ts>" badge. `myOpportunities.service.js`: per-channel candidate sub-pool when `q` is set. `Sidebar.jsx`: standalone "Dashboard" link hidden for admins (Mission Control = single home). `docker-compose.prod.yml` passes through `OIED_KEYWORD_TRENDS_ENABLED` + `OIED_KEYWORD_TRENDS_CRON`.
  - Verification: 11 new tests in `tests/oied/keywordTrendCompute.test.js` (isIndustryPhrase, toDisplayWord, validateMatchCount, validateToolCount, runKeywordTrendCompute happy + tool-only-keep + zero-drop) + full OIED suite passes (32 suites, 454 tests). Syntax-checked all touched JS via `node -c` and the JSX via babel parser. Prod deploy + migration + cron arming below.
  - Notes: Cron defaults OFF (set `OIED_KEYWORD_TRENDS_ENABLED=true` in `.env.prod` and either wait for next 05:15/17:15 UTC slot or hit `/keywords/recompute` to seed). Read handler tolerates pre-migration state — falls through to live aggregator if `keyword_trends` table missing.

## Governance & docs

- [x] CLAUDE.md hardening: add 9 production-grade sections (Contract Enforcement, Modular Composition, Production Readiness 12-Factor, Idempotency & Replayability, Failure-First Design, Build-Break-Harden Loop, Test Strategy Framework, Observability Framework, Security Enforcement Layer)
  - Date: 2026-05-05
  - What changed: Additive-only edits to `CLAUDE.md`. 14 existing H1 sections preserved; 9 new H1 sections inserted at planned anchors. File grew 383 -> 639 lines.
  - Verification: section-count check (23 H1 headers in expected order); manual cross-section consistency check (Failure-First retries vs Stall infinite-retry-prohibited reconciled via explicit bounded backoff)
  - Notes: Created `PROGRESS.md` itself in the same session per the catch-up rule (Logging section). Prior commits (5d16d5c initial release through c3cc622 OIED v8) are not back-filled.

## OIED v9.1 UX bundle - dashboard, drill-down modals, lifecycle-aware actions, rendered drafts (commit 3f92cf0)

- [x] Address all 6 themed fixes from Ali's round-1 walkthrough feedback: new /admin/oied dashboard, sidebar restructure (Discover/Pursue/Operate), reusable OpportunityDetailModal wired into 3 list pages, lifecycle-aware OpportunityActionButtons (hide proposal/offer when blocked + Review Queue link on success), markdown->HTML rendering in Review Queue with sanitized custom renderer, briefing recipient + daily cron config
  - Date: 2026-05-06
  - What changed: 3 new frontend files (OIEDDashboardPage, OpportunityDetailModal, safeMarkdown util) + 6 modified (App.jsx route, Sidebar.jsx restructure, ActionButtons lifecycle gate, ExecutionQueue/MyOpps/Recommendations modal wiring, ReviewQueue markdown render, index.css review-prose styles). docker-compose.prod.yml passes through OIED_BRIEFING_* and OIED_AUTO_TRIGGERS_* env vars; .env.prod sets OIED_BRIEFING_TO=ali@colaberry.com and arms both crons (BRIEFING_CRON 0 7 * * *, AUTO_TRIGGERS_CRON 0 6 * * *).
  - Verification: react-scripts build compiled clean (+27.94 KB JS, +1.65 KB CSS gzipped); container env vars confirmed via `docker exec op-backend node -e "process.env.OIED_BRIEFING_TO"`; backend logs confirmed both schedulers started ("OIED briefing scheduler started cron=0 7 * * *", "OIED trigger engine scheduler started cron=0 6 * * *"); /admin/oied dashboard route returns HTTP 200; v2 walkthrough HTML at docs/oied-feature-walkthrough-v2.html with 8 fix sections, 32 checkboxes, 0 broken anchors, 0 broken images, V2 AUDIT CLEAN.
  - Notes: The "Email me this" briefing button takes 30-60s on prod due to AI compose; nginx times out at 60s but Mandrill delivery completes async. Trigger engine cron defaults to dryRun=true; flip OIED_AUTO_TRIGGERS_DRY_RUN=false later if real-action mode is wanted.

## OIED documentation - feature walkthrough HTML for Ali

- [x] Add docs/oied-feature-walkthrough.html: self-contained HTML walkthrough documenting v1-v9 features with embedded screenshots, per-feature feedback boxes, status checkboxes, end-to-end use-case flows, External AI Bridge contract, and localStorage-backed feedback persistence with copy-to-clipboard
  - Date: 2026-05-06
  - What changed: New `docs/oied-feature-walkthrough.html` (single self-contained file, embedded CSS, no CDN, sticky TOC, executive navy/white styling). 18 PNGs in `docs/oied-walkthrough-assets/` (17 captured pages + 1 final HTML render). New `backend/scripts/capture-oied-walkthrough.js` reuses the existing oied-strategy-screenshot.js login pattern to grab 12 routes + 3 single-opp detail fixtures (16645 direct_submit, 13283 partner_required + send_outreach, 11048 grounding + lifecycle precedence) + bundle disclosures. New `backend/scripts/validate-oied-walkthrough.js` opens the HTML in headless Chromium and asserts DOM counts.
  - Verification: validation script run output --
      Feature sections: 20, use-case sections: 5, textareas: 26, checkboxes: 80 (= 4 per feature x 20),
      TOC links: 29, sections with id: 29, broken anchors: 0, failed image loads: 0, network broken images: 0,
      final screenshot: docs/oied-walkthrough-assets/walkthrough-final.png (10371.4 KB),
      AUDIT CLEAN
  - Notes: Read-only browsing of prod for screenshot capture (no form submits / no POSTs). No real API key in the HTML -- placeholder only. localStorage persistence is per-browser, not server-backed; "Copy all feedback" generates a markdown dump for sharing.

## OIED v9 - partner-based execution mode (commits 62d9e57, 4c6d4de)

- [x] Add execution_mode classifier + partner_profile + outreach_ready to the OIED context envelope; gate proposal-generation recommendations behind capability-fit so opportunities Colaberry can't deliver as prime route to a teaming-partner path instead
  - Date: 2026-05-06
  - What changed: New `executionMode.service.js` (pure decision rule based on operational-blocker / support-layer / direct-fit keyword matching plus profile-service overlap), new `partner.service.js` + `partner.controller.js` (findCandidatePartners + composeOutreachDraft), two new bridge-gated routes (POST /partner-search, POST /partner-outreach), v9 fields threaded through `intelligence.context.js` envelope and both single-row + list endpoints. Two new `recommended_action` values (`find_partner`, `send_outreach`) override the baseline action for pre-draft opps only; lifecycle states (draft, approved, submitted, responded, won, lost) are not touched. v9.1 patch added a profile-services baseline support signal (>=3 services in profile = +2 baseline support) so real-world operational-copy RFPs classify as `partner_required` instead of `ignore`.
  - Verification: 388 OIED tests passing (+40 net new for v9); 1203 backend tests across 98 suites passing; 4 prod smoke scenarios passed (opp 11048 waste -> partner_required/Utah/waste_management/outreach_ready=true, opp 11038 roofing -> partner_required/construction, recommendations endpoint surfaces v9 fields per row with `find_partner` override firing on pre-draft "Armed Security Guards" opp, direct-fit opps unchanged)
  - Notes: schema_version stays at 1 (additive). No migration. No partner_candidates registry yet -- v1 returns empty candidates list and outreach drafting takes a human-supplied prime_name. Sending stays human-gated via accelerator-backend Mandrill.

## OIED v8 - grounded, lifecycle-aware proposal generation (commit c3cc622)

- [x] Add `backend/src/oied/grounding.service.js` and `backend/src/oied/approvedAssets.service.js`; restructure `actionGenerator.service.js` to gate on grounding + lifecycle before AI call
  - Date: 2026-05-05
  - What changed: Proposal generation now derives agency_name + solicitation_id + scope_summary + submission_requirements from opp data, blocks on terminal events (submitted/responded/won/lost), and rejects with 422 needs_context when required fields are missing. Banned-term scan post-generation. `metadata.template_used = 'proposal-v8'`. Additive `context.grounding` sub-object on single-row opportunity endpoint.
  - Verification: 348 OIED tests passing (40 net new); full backend suite 1165 tests across 96 suites passing; 5/5 prod smoke scenarios passed (single-row context grounding visible, lifecycle gate blocks regen, 422 needs_context, happy path content cites U3P + MP26040, post mark-submitted regen blocks)
  - Notes: Schema version stayed at 1 (additive). No migration. Initial deploy used wrong compose file (dev `docker-compose.yml` instead of `docker-compose.prod.yml`); recovered via teardown + bring-up via prod compose with `--env-file .env.prod`. ~4 min prod downtime, no data loss.
